# oth-knowledge — moved to its standalone repository

The shared engineering memory for all othoth77 projects lives in its own repository:

**https://github.com/othoth77/oth-knowledge** (private) — branch `main`.

That repository is the **canonical source of truth**. Do not recreate, copy, or update engineering-memory content here — read and write it there.

## Mandatory workflow (summary — full version in the canonical README)

**BEFORE any development task, in any project:** identify the project/repository → read its entry in `PROJECTS.md` → search `PROBLEMS.md` for similar previous problems → search `LESSONS.md` for relevant knowledge → inspect the referenced real files → reuse existing solutions → only then implement.

**AFTER any meaningful task:** record what changed and why, exact files/folders, important commands, problems with root causes and solutions, verification performed, and important discoveries — in the canonical repository.

## History

The content was originally built and validated inside this repository on 2026-08-18 (this branch, commit `81fd2bf` — the full four-file copy is preserved at that commit) and promoted to the standalone repository the same day (root commit `67b3a88`). The creation record, including why it briefly lived here (the GitHub App integration cannot create repositories), is documented in the canonical `PROBLEMS.md`.
