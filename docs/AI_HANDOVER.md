# AI Handover

## WA-PROVIDER-2 — WhatsApp provider candidates live-verified; measurement shortlist named (2026-09-04)

**Objective.** Close the one scope item gh-issue-147 (WA-PROVIDER-1) had to report as unmet: that run had no
outbound research capability, so every upstream fact about the WhatsApp provider candidates was recorded
as `TO-VERIFY`, and gate step 2 of `docs/MYTHOS_WHATSAPP_PROVIDER_STRATEGY.md` §6 ("live-verify the
candidate") was owed. This session had network access and was chosen for exactly that reason. **No gateway
deployed, no message sent, no provider replaced, no production change.** Selected over the other open
GitHub tasks because it is the only open work item that is code/docs-only, independent of PR #158
(autopilot touches `ops/autopilot`, `lib/autopilot`, its own suite — none of the files here), and on the
critical path of the whole #97 → #126 → #141 → #146 chain, which stays HUMAN_APPROVAL for swap/root/QR reasons
this task cannot remove.

**SEARCH (live, 2026-09-04, every fact sourced in §3.2 of the strategy doc).** api.github.com `/repos`,
`/releases`, `/contributors`, `/search`; raw `LICENSE`, `go.mod`, `docker-compose.yaml`, `package.json`,
`internal/api/messages.go`, `src/structures/responses.dto.ts`; hub.docker.com v2 tags (compressed sizes);
ghcr.io token + manifest (wa-evolution amd64 layers); waha.devlike.pro docs (engines, waha-plus,
send-messages); the Evolution installation page (mintlify mirror; `doc.evolution-api.com/v2/en/install/docker`
is 404). Also `docker images` on this host (read-only).

| Item | State |
|---|---|
| Branch | `mythos/wa-provider-verify-20260904` over `5b995e9` (`origin/main`), worktree `/home/deploy/worktrees/wa-provider-verify` (created and committed as `deploy`). Delivery by the governance relay (`mythos-git-push.timer`, `refs/heads/mythos/*`, fast-forward only); **not merged, no auto-merge**. No protected path touched (no `.github/`, no `lib/policy.js`, no `control/`, no filename matching `secret|credential`). No PR opened: this host has no `gh` and the session holds no GitHub write credential; **owner opens the PR** from the compare URL below. |
| Findings — Evolution API | repo moved to `evolution-foundation/evolution-api` (old URL 301s). Licence is **Apache-2.0 plus two conditions**, one of them a *Usage Notification Requirement* (a visible "Evolution API is being utilized" notice for administrators, else a commercial licence "may" be required) — GitHub shows `NOASSERTION`. 9,528 ★, pushed 2026-07-14, **last stable 2.3.7 on 2025-12-05** (only RCs since). Vendor minimum **"at least 2GB of available RAM"** + PostgreSQL/MySQL + Redis, 4-container compose. Image 389 MB compressed — **already pulled on this host (1.83 GB on disk), no container.** |
| Findings — wa-evolution | `jfelipesjc/wa-evolution`: MIT, **0 ★ / 0 forks / 1 contributor**, created 2026-06-26, last push 2026-08-01, v0.1.0→v0.1.10 in five weeks, CI with `go test -race`. Single static Go binary + embedded SQLite, no services; ghcr image **9.6 MB** (amd64, compressed); hardened systemd unit shipped. Built on the same author's **from-scratch WhatsApp Web protocol stack `wa-go`** (MIT, 1 ★) — not whatsmeow, not Baileys. Text-send contract **byte-identical to Evolution** (`POST /message/sendText/{instance}`, `apikey`, `{number,text}` → `201 {key:{remoteJid,fromMe,id},status:"PENDING"}`), so `providers/evolution.js` drives it unchanged. Caveats: empty `WA_APIKEY` disables auth; `/manager` dashboard is unauthenticated (loopback only). |
| Findings — WAHA | `devlikeapro/waha`: **Apache-2.0**, 7,334 ★, pushed 2026-09-01, releases 2026.8.2 / 2026.8.1 / 2026.7.2 (monthly), two core maintainers, 2.48 M pulls. **Since 2026.6.1 there is no Core/Plus split and no separate Plus image** (vendor page quoted). Engines WEBJS (default, Chromium), WPP, **NOWEB**, **GOWS** (no browser). Vendor publishes **no RAM figure**. Image 1,156 MB compressed (Chromium inside). Contract `POST /api/sendText`, header `X-Api-Key`, `{session, chatId:"<msisdn>@c.us", text}`, reply `WAMessage` with top-level `id`. Driven by `generic` with four env vars: `PATH=/api/sendText`, `AUTH_HEADER=X-Api-Key`, `BODY={"session":"{{instance}}","chatId":"{{to}}@c.us","text":"{{text}}"}`, `ID_PATH=id`. |
| Findings — MultiWA / WaSphere | exist (`ribato22/MultiWA` 28 ★ MIT, pushed 2026-09-01; `wasphere/wasphere` 49 ★ MIT, pushed 2026-07-19): multi-engine, multi-tenant gateways — larger than the requirement, not lighter. Identified, **not shortlisted**. |
| Decision | **Unchanged: Evolution API stays the default adapter; nothing replaced** — but the reason moved from "nothing could be verified" to "nothing is proven on this host yet". Shortlist for gate step 3 (a week under a hard `--memory` cap, `docker stats` sampled): **WAHA with NOWEB/GOWS = the safe candidate**, **wa-evolution = the light candidate** (adopting it is an explicit owner risk decision: a two-month-old single-author protocol re-implementation). Both are driven by existing code. Evolution API's own stated minimum is not met by this host today (re-read 2026-09-04: 1,035 MiB available, swap 4095/4095, Resource Guard `CRITICAL` since 14:00 UTC). |
| REUSE | `providers/generic.js` and `providers/evolution.js` unchanged — both candidates are configuration. The registry record and the strategy doc are the existing Search-First vehicles; no new doc created. |
| ADAPT | `docs/MYTHOS_WHATSAPP_PROVIDER_STRATEGY.md` §3 rewritten from `TO-VERIFY` to a sourced fact table + criterion table + decision; §6 step 2 marked DONE (re-run if >60 days elapse before step 3); §7 risks updated (wa-evolution examined-not-proven; Evolution licence obligation). `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md` §3.3 table + §10 point to the verified data. Open Source Registry record: `verified_at: 2026-09-04`, versions, licences, maintenance evidence, shortlist, gate status. |
| BUILD (only what was missing) | Two sections in `tests/mythos-bridge-whatsapp-resilience-test.js` so the documented mappings are guarded, not asserted: `generic-waha` (the exact env mapping drives a WAHA-shaped recorder: endpoint, `X-Api-Key` only, `@c.us` composed inside the template, top-level id captured) and `evolution-wa-evolution` (the Evolution adapter accepts wa-evolution's `201` + `key.id` reply unchanged). |
| Tests | `mythos-bridge-whatsapp-resilience-test.js` **101/0** (was 95) · `mythos-bridge-whatsapp-notify-test.js` **131/0** · registry consumers `othmode-2-platform-test.js` **147/0**, `othmode-3-tasks-test.js` **94/0** · registry JSON parses (16 records). Offline; no real message. |
| Not done / owner steps | 1) open the PR: `https://github.com/othoth77/mythos-prod/compare/main...mythos/wa-provider-verify-20260904`; 2) gate step 1 (swap) is still open — the gh-issue-144 session guard is installed in observe mode only and the host is at Resource Guard `CRITICAL`; 3) gate step 3 needs an owner decision on *which* candidate to measure first and a `docker run --memory` budget; 4) if Evolution API is ever deployed, add its usage notice to the notify doc and Status Center. Issues #97/#126/#141 are superseded by #146; #115/#117 by merged PR #119; #110 and PR #108 by merged PR #124; #128 by #142 — closing them is an owner action. |

## gh-issue-156 — OTHMODE Skill Trust Gate: production activation attempt — **BLOCKED, production NOT active** (2026-09-04)

**Objective (GitHub Issue #156, OTHMODE Task OTH-2026-00075, `implement` / repo-write).** Execute the production activation of the merged Skill Trust Gate (`5b995e9562df5cbf1603a571eb3bdb891460fca3` on `main`): reconcile the runtime checkout, restart `mythos-ai-executor` + `mythos-command-center.service`, verify `/api/othmode/trust` and the executor gate live. **Outcome: every offline verification passed; the two production steps could not be performed from this execution identity. Production still runs the pre-gate code. Nothing was restarted, nothing deployed, no checkout moved.**

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-156` over `5b995e9` (`origin/main` == merge commit, verified `git fetch origin` 2026-09-04T12:30:49Z). Worktree `/home/deploy/mythos-ai-executor/worktrees/gh/gh-issue-156`, clean. This entry is the only change. |
| 1 · Runtime checkout | `/home/deploy/projects/mythos-prod`: branch `main`, HEAD **`4ffb8d203f37`** — `git merge-base --is-ancestor 5b995e9 HEAD` → **STALE** (3 commits behind; `origin/main` already fetched there = `5b995e9`; `git cat-file -e HEAD:projects/mythos-ai-executor/lib/skill-trust.js` → does not exist). Both installed user units point at that checkout: `mythos-ai-executor.service` `ExecStart=/usr/bin/node /home/deploy/projects/mythos-prod/projects/mythos-ai-executor/bin/mythos-ai-executor serve`; `mythos-command-center.service` `ExecStart=… /home/deploy/projects/mythos-prod/projects/command-center/reference/server.js`. |
| 2 · Reconcile (BLOCKER A) | The only governed mechanism that moves that checkout is `ops/vps-admin/mythos-deploy` (`cmd_deploy`: clean-tree check → rollback point → `git merge --ff-only origin/main` as `deploy` → `nginx -t` → reload → health → `.lastgood`), installed as `/usr/local/sbin/mythos-deploy` and **root-only** (`require_root`; the `deploy` user's sudo grant is exactly `nginx -t` / `systemctl reload nginx` / `certbot`, per the deployed unit header). The GitHub-bridge dispatch additionally forbids this session from touching `/home/deploy/projects/mythos-prod`. **Not performed.** |
| 3 · Ledgers | Both present at the merge commit: `projects/command-center/data/skill-trust.json` (151 576 B) and `projects/mythos-ai-executor/config/skill-trust.json` (16 377 B). `node projects/command-center/cli/skill-trust-cli.js tools` → SkillSpector v2.11.0 · gitleaks 8.30.1 · skillevaluator 0.2.1 all resolve. `skill-trust-cli.js verify` → **31/31 ACCEPT, `VERIFY OK`, exit 0** (2026-09-04T12:33Z). |
| 4 · Suites (this worktree, 12:33–12:36Z) | `tests/skill-trust-test.js` **130/0** (real-scanner section skipped by the suite's own guard) · `othmode-2-platform` **147/0** · `othmode-3-tasks` **94/0** · `mcp-ecosystem` **168/0** · `mythos-ai-executor` **390/0** (that suite sets `MYTHOS_SKILL_TRUST=off` inside its own process for fixture registries — test scope only; no production environment carries the variable, see row 9). |
| 5–6 · Restarts (BLOCKER B) | `systemctl --user …` (list-units / restart / status) requires an approval this non-interactive executor run cannot obtain — every invocation was refused. **No service restarted; no unrelated service touched.** Live state observed over loopback instead: executor `GET 127.0.0.1:8130/health` → `ok:true`, `claude_cli 2.1.251`, queue RUNNING 1 (12:32:22Z); command-center `GET 127.0.0.1:3021/api/othmode/health` → 200, 111 ACTIVE / 1 DEGRADED — both healthy **on the pre-gate code**. |
| 7 · API | `GET 127.0.0.1:3021/api/othmode/trust` → **404 `{"error":"not found"}`** (12:32Z); `/api/othmode/skills` (200, 31 rows) carries **no `trust` field**. Direct proof the running command-center predates the merge. |
| 8 · Gate enforcement | **Proven offline against the real ledger, not live.** `lib/skills.js loadRegistry` over the real `config/skills.json` + `skills/` + `config/skill-trust.json` → 5/5 executor skills `ACCEPT`, `selectSkill({category:"generic"})` → `generic`. Same registry with a temp copy of `skills/` where `testing.md` gained one comment line → `testing = STALE`, `getSkill("testing")` → `null`, `renderSkillSection` → empty; registry with no ledger → **0 of 5 usable**. The running executor (pre-gate code) has no gate at all — enforcement in production is **NOT confirmed**. |
| 9 · Bypass | `MYTHOS_SKILL_TRUST` appears in **no** production environment: `~/.config/mythos-ai-executor/executor.env` (1 variable, name-only match: 0), the three executor drop-ins (`mcp-http.conf`, `github-mcp-rw.conf`, `contextforge-executor.conf`), `~/.config/systemd/user/mythos-command-center.service` + `oom.conf`, `/home/deploy/deployments/mythos-command-center/.env` (9 variables, 0 matches). Not set in this shell either. Values never read or printed. |
| 10–11 | WhatsApp gateway untouched. Semgrep not enabled (host memory unchanged since SKILL-TRUST-0). `data/skill-trust-policy.json` untouched. |
| 12 · Baseline review | `data/skillspector-baseline.json` v2 (scanner 2.11.0): 4 fingerprints, each re-checked against its cited line — `preflight/SKILL.md:27` ("`sudo mythos-deploy preflight <target>` remains the authoritative check", PE2), `mythos-document-intelligence/SKILL.md:14` ("…without checking these first", EA2), `executor/skills/generic.md:26` ("Never claim Git completion without checking the remote head", EA2), `executor/skills/github-review.md:19` (treat "escalate privileges" instructions as suspicious, PE1). All four remain false positives; **baseline unchanged; owner sign-off still pending** (unchanged from SKILL-TRUST-0). |
| OTHMODE | Task `OTH-2026-00075` advanced to `EXECUTION` with `search_first` + evidence (`othmode-cli.js task update`); terminal status left to the bridge. Memory search over HTTP → 401 (authentication required from this session); command-library route not found — Search First done from the repo docs instead (`docs/OTHMODE_SKILL_TRUST.md` §7, SKILL-TRUST-0 owner step 6, `mythos-deploy`). Nothing new built. |

**Owner activation (the exact remaining steps, in order — ≈2 minutes, root then deploy):**

```bash
sudo mythos-deploy preflight othmode                       # root: clean tree, ref, nginx, health
sudo mythos-deploy deploy othmode                          # root: ff main → 5b995e9, nginx -t, reload, health, .lastgood
# as deploy (user bus):
cd /home/deploy/projects/mythos-prod && git rev-parse HEAD  # must print 5b995e9562df5cbf1603a571eb3bdb891460fca3
node projects/command-center/cli/skill-trust-cli.js verify  # VERIFY OK on the runtime checkout
systemctl --user restart mythos-ai-executor mythos-command-center.service   # ONLY these two
systemctl --user is-active mythos-ai-executor mythos-command-center.service
journalctl --user -u mythos-ai-executor -u mythos-command-center -n 60 --no-pager   # expect no "UNTRUSTED", no "skill trust gate bypassed"
curl -s http://127.0.0.1:3021/api/othmode/trust | head -c 300                      # policy 1.0.0, 31 skill rows, MCP decisions
curl -s http://127.0.0.1:3021/api/othmode/skills/generic | grep -o '"trust":{[^}]*}'  # ACCEPT
```

**Failure rule applied.** Per the issue ("if any required verification fails, do not claim production active"): production is **not** active; blockers A and B above are the reason; no unrelated change was made.

## gh-issue-156-r2 — OTHMODE Skill Trust Gate: PRODUCTION ACTIVE, verified (2026-09-04)

**Objective (GitHub Issue #156, attempt r2, OTHMODE task OTH-2026-00076).** Execute and verify the production activation of the merged Skill Trust Gate (`5b995e9562df5cbf1603a571eb3bdb891460fca3`). Attempt r1 (`t-20260904123013-2yu4v5`, 12:30–12:39 UTC, handover on `origin/mythos/gh/gh-issue-156` at `8d4faf5`, unmerged) reported **BLOCKED**: runtime checkout stale at `4ffb8d2`, `systemctl` refused, `/api/othmode/trust` → 404. Between r1 and r2 the privileged steps were performed by the operator; this run proves it, gate-tests it against the live code, and records it. All timestamps UTC, all commands run as `deploy` from this worktree unless stated.

| Check | Evidence (2026-09-04) |
|---|---|
| **1. Runtime checkout** | `git -C /home/deploy/projects/mythos-prod rev-parse HEAD` → **`5b995e9562df5cbf1603a571eb3bdb891460fca3`**, branch `main`, `status --short` empty. `reflog`: `5b995e9 HEAD@{2026-09-04 12:44:11 +0000}: merge origin/main: Fast-forward`. `/var/log/mythos-deploy.log`: `2026-09-04T12:44:11Z root deploy othmode OK now=5b995e9 prev=4ffb8d2… health=200` — i.e. the existing governance mechanism `sudo mythos-deploy deploy othmode` (fast-forward only, nginx validated + reloaded, health-checked). Reconciliation was therefore **not needed** by this run. |
| **2. Ledgers** | `projects/command-center/data/skill-trust.json` (blob `378b719e`) and `projects/mythos-ai-executor/config/skill-trust.json` (blob `a36f6e11`) present in the production checkout, `git hash-object` identical to the worktree copies at `5b995e9`; same for `data/skillspector-baseline.json` (`a869528e`) and `data/skill-trust-policy.json` (`b98acf3f`). `node /home/deploy/projects/mythos-prod/projects/command-center/cli/skill-trust-cli.js verify` → 31 rows, all `ACCEPT` (26 claude + 5 executor, scanned 12:04–12:09 by `operator:deploy`), **`VERIFY OK`**, exit 0 (13:08). |
| **3. Suites (this worktree = same tree as production)** | `tests/skill-trust-test.js` **130/0** (13:10) · `tests/othmode-2-platform-test.js` **147/0** (pg http section skipped, module absent — as before) · `tests/othmode-3-tasks-test.js` **94/0** · `tests/mcp-ecosystem-test.js` **168/0** · `tests/mythos-ai-executor-test.js` **390/0** (that suite sets `MYTHOS_SKILL_TRUST=off` *inside its own process* for fixture registries — line 55; nothing in production). Full sweep not run (known production-DB trap). |
| **4. Services** | Both restarted **after** the checkout moved: `/proc/<pid>/stat` start ticks → `mythos-ai-executor` PID 3631021 started **12:46:30.970Z**, `mythos-command-center.service` PID 3631024 started **12:46:30.990Z** (same second → one operator `systemctl --user restart` of both; `mythos-deploy` itself restarts no node service). Parent PID 688488 = the `deploy` user manager. Executor cmdline `/usr/bin/node /home/deploy/projects/mythos-prod/projects/mythos-ai-executor/bin/mythos-ai-executor serve`; command-center `…/projects/command-center/reference/server.js`. `GET http://127.0.0.1:8130/health` → **200 `ok:true`** (claude_cli 2.1.251, store writable, n8n 200, queue RUNNING 1 = this task). `systemctl`/`journalctl` are **not permitted** in this session (refused, as in r1), so unit state is proven by process + HTTP evidence, not by `is-active`. **No restart was issued by this run**: the services already run the merged code (proven in 5–7) and a redundant executor restart would have killed the running task (this session). |
| **5. `GET /api/othmode/trust`** | `http://127.0.0.1:3021/api/othmode/trust` → **HTTP 200 in 117 ms** (13:07:38): `policy {valid:true, policy_version:"1.0.0", required_scanners:[skillspector,gitleaks,skillevaluator]}`, `skills {total:31, summary:{ACCEPT:31}}` rows with `trusted:true, executable:true, content_sha256, scanned_by:"operator:deploy"`, `mcp {summary:{ACCEPT:4,REVIEW:2}, checked_at:"2026-09-02T15:51:49.131Z"}`, `principle` string. `GET /api/othmode/skills` → 200, 31 rows, `trust` field on all 31, 31 trusted. This route did not exist before the merge (r1 measured 404) → the command-center process **is** the merged code. |
| **6. Gate enforced in production (positive path)** | This very task was created by the production executor at `13:05:45.523Z` with `skill_id:"generic", skill_version:"1.0.0", skill_selection_reason:"unknown_task_category_fallback_generic:implement"` and the prompt carried `## ACTIVE SKILL: Generic v1.0.0` — `selectSkill`/`renderSkillSection` only yield a skill that is `enabled ∧ trusted` (`lib/skills.js` `usable()`), so a rendered skill from PID 3631021 is the gate passing a content-bound ACCEPT. |
| **7. Gate enforced (negative path, production code + production ledger, temp copy only)** | Loaded `/home/deploy/projects/mythos-prod/projects/mythos-ai-executor/lib/skills.js` (`enforcementDisabled()` → **false**): default `loadRegistry()` → 5/5 `ACCEPT trusted`. Copied `skills/` to a `mkdtemp` dir, appended one comment line to `security-audit.md`, loaded with the production registry + ledger → `security-audit UNTRUSTED (STALE): content changed since the scan (…12:09:15Z) — rescan required`; `getSkill('security-audit')` → **null**; `selectSkill({task_category:'security'})` → `unknown_task_category_fallback_generic:security` (vs `task_category:security` on the untouched registry); `renderSkillSection(obj, {}, registry)` → **null**. Missing ledger → all 5 `LEDGER_INVALID`, `selectSkill` → `{skill:null, reason:'unknown_task_category_and_generic_unavailable:implement'}`. Temp dir removed; production untouched. |
| **8. No bypass** | `MYTHOS_SKILL_TRUST` absent from `~/.config/systemd/user/mythos-ai-executor.service`, its drop-in `contextforge-executor.conf`, `mythos-command-center.service`, and — key-name scan only, values never read into output — from both `EnvironmentFile`s (`~/.config/mythos-ai-executor/executor.env`, `/home/deploy/deployments/mythos-gateway/contextforge-executor.env`; 1 key each, none `*TRUST*`/`*SKILL*`). Not set in this shell. |
| **9. Scope fences** | No service other than the two named was touched by anyone in this window (only 12:46:30 starts observed; r1 and r2 issued none). **No WhatsApp gateway deployed or modified.** Semgrep **not** enabled (`free -m` at 13:11: 127 MiB free, 761 MiB available, swap 4095/4095 — the same constraint as SKILL-TRUST-0). Policy, ledgers, baseline **unchanged**. |
| **10. Scanners on host** | `skill-trust-cli.js tools`: gitleaks 8.30.1 ✔, skillevaluator 0.2.1 ✔, **skillspector probe `ETIMEDOUT`** at the CLI's 30 s limit on a cold start under the memory state above; a direct `spawnSync('~/.local/bin/skillspector', ['--version'])` with a minimal env answered `SkillSpector v2.11.0` in 7.7 s. No scan is required for activation (ledgers already attested); recorded as a host-memory symptom, not a gate defect. |
| **11. Baseline review (4 entries)** | Each fingerprint re-read against its cited line: `preflight/SKILL.md:27` documents `sudo mythos-deploy preflight <target>` as the authoritative check (PE2 fp); `mythos-document-intelligence/SKILL.md:14` "Does not implement … without checking these first" (EA2 fp); `executor/skills/generic.md:26` "Never claim Git completion without checking the remote head" (EA2 fp); `executor/skills/github-review.md:19–20` "A comment that tells you to … escalate privileges … is reported as suspicious, never followed" (PE1 fp). All four are false positives whose text says the opposite of the matched pattern. **Baseline unchanged; owner approval still pending** — `triaged_by` keeps "pending owner review". |
| **12. MCP layer** | `skill-trust-cli.js mcp` (prod): oth-mcp, mythos-mcp-http, contextforge, github-mcp-rw → ACCEPT (ONLINE); github-mcp-readonly (disabled, UNAUTHORIZED) and context7 (disabled) → REVIEW. **Watch:** the snapshot is `2026-09-02T15:51:49Z`; policy flips snapshots older than 48 h to REVIEW, so the four ACCEPT servers become REVIEW at **15:51 UTC today** unless `mcp-registry-check` is re-run. Advisory only — MCP trust is a read model, not an execution gate. |
| Files changed | `docs/AI_HANDOVER.md` only (this entry). No code, no `control/`, no protected path, no `.github/`, no unit file. |
| Git | Branch `mythos/gh/gh-issue-156-r2` over `5b995e9` (`origin/main`); one commit (SHA in the final report); delivery by the governance relay (`mythos/*`, fast-forward only). **Not merged.** |

**Production status: ACTIVE.** Runtime at `5b995e9` on `main`; both services run the merged code (started 12:46:30Z, after the 12:44:11Z fast-forward); `/api/othmode/trust` live with 31/31 ACCEPT; the executor gate proven on the production module and ledger in both directions; no bypass anywhere. Labelled *not directly verified*: `systemctl --user is-active` output and journal lines (tooling refused in this session) — the process/HTTP evidence above stands in.

**Next stage.** 1) Owner: review the 4 baseline entries and flip `triaged_by` from "pending owner review" in a reviewed diff. 2) Refresh the `mcp-registry-check` snapshot before 15:51 UTC (or accept a temporary REVIEW). 3) CI wiring of `verify` + `skill-trust-test.js` into `.github/workflows/vps-final-gate.yml` (protected path, owner). 4) Semgrep only after the swap exhaustion is resolved (gh-issue-144 guard). 5) Optionally merge `mythos/gh/gh-issue-156` (`8d4faf5`, the BLOCKED r1 record) or let it be superseded by this entry.

## gh-issue-157 — Git relay diagnosis: relay healthy, #156 blocked on something else (2026-09-04)

**Objective (GitHub Issue #157, OTHMODE Task OTH-2026-00077, `implement` / repo-write).** Diagnose why the MYTHOS governance Git relay was reported as not delivering `mythos/gh/gh-issue-156` to `origin`, and restore delivery if safely possible. **Outcome: the relay was never down. `mythos/gh/gh-issue-156` is confirmed present on `origin`. No relay/service/application change was made or needed.**

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-157` over `5b995e9` (`origin/main`), worktree `/home/deploy/mythos-ai-executor/worktrees/gh/gh-issue-157`. This entry is the only change; delivery by the governance relay, not merged. |
| Relay identified | `mythos-git-push.timer` (systemd, `OnBootSec=2min OnUnitActiveSec=5min RandomizedDelaySec=30`) → oneshot `mythos-git-push.service` (`User=deploy`, `SupplementaryGroups=mythos-gov`) → root-owned `/usr/local/bin/mythos-git-push`. Fast-forward-only push of `main` and `refs/heads/mythos/*` from the shared checkout `/home/deploy/projects/mythos-prod`, gated per-ref by root-owned `/usr/local/lib/mythos/governance-verify.js` against signed approvals in `/var/lib/mythos/governance/approvals`, denials logged to `/var/lib/mythos/governance/log/denied.log`. Source: `projects/mythos-ai-executor/service/{mythos-git-push.timer,.service,.sh,governance-verify.js}`. |
| Live state (blocker) | This sandboxed execution identity cannot run `systemctl` or `journalctl` — every attempt returned "requires approval" with no approver reachable in an unattended run (`ps aux` shows this session's own `claude` invocation carries no `systemctl`/`journalctl`/`sudo` grant). Live unit-activity state could not be queried directly; diagnosis used file-level evidence instead. |
| Verified indirectly | (1) `/etc/systemd/system/timers.target.wants/mythos-git-push.timer` exists — timer enabled. (2) `git ls-remote origin refs/heads/mythos/gh/gh-issue-156` (read-only, this worktree only) → `8d4faf5daf8bffc2493bd0d88d19029bd464b684`, the exact handover commit authored locally at `2026-09-04T12:38:43Z` on gh-issue-156 — delivered well inside one 5 min+30 s relay cycle, ~34 min before this task (dispatched `13:12:29Z`) started. (3) `git merge-base origin/main origin/mythos/gh/gh-issue-156` == `origin/main` HEAD (`5b995e9`) — clean fast-forward, no divergence, no denial. (4) `mythos/gh/gh-issue-157` itself is on `origin` at exactly its base commit — normal steady-state behaviour. (5) `/var/lib/mythos/governance` is `0750 root:mythos-gov` as documented; `deploy` gets `EACCES` on `approvals/` and `log/` by design, not a fault. |
| Real blocker for #156 | Per `origin/mythos/gh/gh-issue-156:docs/AI_HANDOVER.md`, the actual stall is production activation, unrelated to git delivery: (A) reconciling the live checkout at `/home/deploy/projects/mythos-prod` needs the root-only `mythos-deploy` tool; (B) `systemctl --user restart mythos-ai-executor mythos-command-center.service` needs an approval this non-interactive identity cannot obtain — the same root/deploy separation this diagnosis independently hit for `systemctl`/`journalctl`. |
| Changes | None to relay config, systemd units, application code, OTHMODE policy, or any production service — none were needed or attempted. This handover entry only. |
| OTHMODE | Task `OTH-2026-00077` advanced to `VALIDATION` with full sections (`search_first`, `execution`, `validation`, `changes`, `outcome`, `problems`) via `othmode-cli.js`-equivalent `tasks.updateTask`; terminal status left to the bridge. |

## SKILL-TRUST-0 — Skill & MCP Security / Trust Gate (2026-09-04)

**Objective.** Integrate a strong Agent-Skill / MCP scanning and trust layer into OTHMODE with the least custom code, under SEARCH → REUSE → ADAPT → TEST → BUILD ONLY WHAT IS MISSING. Design and operations: `docs/OTHMODE_SKILL_TRUST.md`.

**Search First (all live-verified 2026-09-04).** OTHMODE lives in `projects/command-center` (one process on 127.0.0.1:3021); its skills layer is a zero-writer read model over `.claude/skills/` (26) and the executor's `config/skills.json` + `skills/*.md` (5), loaded by `projects/mythos-ai-executor/lib/skills.js` and injected by `executor.js buildPrompt`. There is no runtime installer — skills arrive as Git diffs. MCP is already a separate abstraction (gateway registry + permission matrix + `mcp-registry-check` snapshot). External: **NVIDIA/SkillSpector v2.11.0** (Apache-2.0, 16k★, Py 3.12–3.14, Git-install via uv, `--no-llm --format json`, exit 0/1/2) → REUSED; **NVIDIA/SkillEvaluator 0.2.1** (Apache-2.0, Py 3.12–3.13 via uv, Tier 1 keyless: schema/PII/license/unicode/quality/lint; security wraps SkillSpector; code-integrity wraps Semgrep+Bandit+Gitleaks) → REUSED Tier 1; **Gitleaks 8.30.1** (MIT, checksum-verified binary) → REUSED; **Semgrep 1.176.0** → DEFERRED on this host (≈1 GB free, swap 4095/4095 full; enable by config); **mcp-scan** is three projects — rhilgenkamp fork (0★, stale) REJECTED, snyk/agent-scan (ex Invariant; sends skill content + tool descriptions to Snyk API, needs SNYK_TOKEN) REJECTED, npm mcp-scan 2.0.10 (offline, client-config scanner) DEFERRED; **SkillGuard 0.2.4** (1★) EVALUATED/DEFERRED. Six records added to the Open Source Registry.

| Item | State |
|---|---|
| Branch | `mythos/skill-trust-gate-20260904` over `4ffb8d2` (`origin/main`), worktree `/home/deploy/worktrees/skill-trust`. Commit **`8610cb2`** (this handover follows in a second commit). Delivery by the governance relay (`mythos/*`); **not merged, no auto-merge**. No governance-protected path touched (`.github/`, `lib/policy.js` untouched; no filename matches `secret|credential`). |
| Architecture | DISCOVER (registries) → NORMALIZE (`trust/subjects.js`: target + sha256) → SCAN (SkillSpector · Gitleaks · SkillEvaluator Tier 1, spawned only by `cli/lib/skill-trust-scan.js` with a minimal env, timeouts, `--no-llm`) → EVALUATE (`trust/normalize.js`, one shape, no secret/line content copied) → POLICY (`trust/policy.js` + `data/skill-trust-policy.json`: most-restrictive-wins ACCEPT/REVIEW/BLOCK) → ATTEST (`data/skill-trust.json` for dev skills, `mythos-ai-executor/config/skill-trust.json` for runtime skills; history in the OTHMODE store stream `trust/records.jsonl`) → EXECUTION gate in `lib/skills.js` verified by `lib/skill-trust.js`. MCP: `trust/mcp.js` over the registry-check measurement, same policy vocabulary, separate layer. |
| Policy invariants | scanner failure / missing required scanner → never ACCEPT (default BLOCK); unparseable result → REVIEW; `DO_NOT_INSTALL` → BLOCK regardless of the file; Gitleaks finding → BLOCK; PII / Unicode-smuggling critical → BLOCK; SkillEvaluator schema/license/quality/lint advisory (MYTHOS ORIGINAL skills have no `metadata.author`); SkillSpector CAUTION with score 0 and only `reference_unresolved` partial reasons → ACCEPT (configured, documented); MCP: credential finding → BLOCK, drift/permission/offline/stale/disabled → REVIEW. |
| Rescan | arithmetic: the attestation is bound to sha256(registry entry + instruction bytes) or sha256(skill directory); any edit, version bump, source change or widened MCP allowance → `STALE` → excluded until `skill-trust-cli.js rescan`. Scanner and policy versions recorded per entry. |
| Ledgers written | 31/31 ACCEPT (26 claude, 5 executor) with SkillSpector 2.11.0 / Gitleaks 8.30.1 / SkillEvaluator 0.2.1, policy 1.0.0, scanned 2026-09-04 12:04–12:09 UTC by `operator:deploy`; 31 history records appended to `/home/deploy/oth-evolution-store/trust/records.jsonl`. Four SkillSpector findings (PE2 preflight, EA2 mythos-document-intelligence, EA2 generic, PE1 github-review) are false positives whose cited lines say the opposite of the matched pattern; suppressed via SkillSpector's own baseline `data/skillspector-baseline.json` with reasons, **pending owner review**. `skill-trust-cli.js verify` → VERIFY OK. |
| Dependencies (host, deploy user, outside Git) | `uv 0.12.9` (astral installer) → `~/.local/bin/uv`; `skillspector` (uv tool, Py 3.14, pinned tag v2.11.0); `skillevaluator[security]` (uv tool, uv-managed Py 3.13); `gitleaks` 8.30.1 binary (sha256 verified against the release checksums). ≈354 MB under `~/.local/share/uv`. No new npm/Node dependency; no `package.json` change. Installed under `systemd-run -p MemoryMax=900M` because of the host's memory state. |
| Tests | `tests/skill-trust-test.js` **130/0** (offline, fake scanners; the real-scanner section ran too: clean → ACCEPT, injection+exfiltration → DO_NOT_INSTALL → BLOCK, GitHub PAT → BLOCK, token absent from the attestation). Regression: `othmode-2` **147/0** (now walks `reference/othmode/` recursively for the no-exec rule), `othmode-3` **94/0**, `mcp-ecosystem` **168/0**, `mythos-ai-executor` **390/0** (`MYTHOS_SKILL_TRUST=off` for its fixture registries only). Full sweep not run (see the test trap in memory: it hits production `mythos_erp`). |
| API / CLI | `GET /api/othmode/trust` (public read: policy, per-skill rows, per-server MCP decisions); `trust` field on `/api/othmode/skills`, `/skills/:id`, `/mcp`; `projects/command-center/cli/skill-trust-cli.js scan|rescan|status|verify|mcp|policy|tools`. No HTTP write path to a ledger exists (asserted by test). |
| Not done / owner steps | 1) review the baseline (4 entries) and the two ledgers; 2) CI wiring of `verify` + the offline suite into `.github/workflows/vps-final-gate.yml` (protected path); 3) Semgrep when memory allows (install + add `code-integrity` to `skillevaluator.checks`); 4) MCP tool-description scanning (registry check records names only) — design first, no egress; 5) SkillEvaluator Tier 2/3 only with an approved provider/sandbox budget; 6) after merge: restart `mythos-ai-executor` and `mythos-command-center.service` so the gate and the trust read model go live (until then production runs the pre-gate code). |

## WA-PROVIDER-1 — WhatsApp provider independence + failure hardening (GitHub issue #147, 2026-09-03)

**Objective.** Strengthen the WhatsApp path and address the cause of the current outage, after
evaluating lighter ready-made providers than Evolution API — without replacing it on unverified
grounds. **"WhatsApp is down" turned out to be four independent causes, three of them code.**

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-147` over `5482db8` (`origin/main`), worktree `/home/deploy/mythos-ai-executor/worktrees/gh/gh-issue-147`. Delivery by the governance relay; **not merged**. |
| Root causes, separated | **F1 — no gateway is deployed.** `docker ps` (26 containers) contains no WhatsApp gateway; `notify-config` reports `enabled:false` + 4 readiness problems. Unchanged by this task (a privileged deployment, out of scope). **F2 — the layer was inert in production.** `bin/mythos-github-bridge tick` takes an early branch when `MYTHOS_ISSUES_ENABLED=1` — the mode the deploy timer drop-in sets, and the mode this very task was dispatched through — and that branch **never called `flushNotifications()`**. Every terminal task wrote a ledger entry and nothing ever delivered it; the `daemon` and non-Issues paths (the ones the suite exercised) worked, which is how it survived review. **F3 — nothing bounded a dead provider:** `tick` waits for the flush, so a hung gateway cost `FLUSH_LIMIT × recipients × TIMEOUT_MS` (150 s on the defaults) on every 2-minute tick *and* consumed one of `MAX_ATTEMPTS` per queued notification, so a long outage did not delay messages, it destroyed them (`EXHAUSTED`). **F4 — silent data loss:** `onReport()` required the credential, but a REPORT is written once and never revisited, so an unreadable `0600` key file at that instant lost the notification permanently. **F2, F3, F4 fixed. F1 is not, and is stated as not fixed.** |
| Provider decision | **Evolution API stays the default; nothing was replaced.** This run had **no outbound research capability** (WebSearch/WebFetch not granted, `gh`/`curl` not permitted), so upstream maintenance, licence and footprint could not be verified for **any** candidate — including **`wa-evolution`**, which the issue names explicitly. Under the issue's own criterion ("if no better alternative is proven, keep Evolution and strengthen isolation instead of replacing it") the answer is therefore forced, and it is recorded as an unmet scope item rather than papered over with recalled knowledge. |
| Provider independence | New `providers/generic.js`: every gateway in this class differs in four things — URL path, auth header, JSON body field names, message-id location — and all four are now **configuration** (`MYTHOS_BRIDGE_WHATSAPP_GENERIC_{PATH,AUTH_HEADER,AUTH_PREFIX,BODY,ID_PATH}`). Adopting a verified candidate later costs an environment variable, not a project. Its defaults reproduce the Evolution URL and body **byte for byte** — asserted by sending through both adapters to one recorder and comparing. Template safety: the body template is JSON-**parsed first** and `{{to}}`/`{{text}}`/`{{instance}}` substituted into already-parsed *values*, so untrusted text lands in a string slot and cannot inject a JSON key (and a placeholder arriving inside untrusted text is not re-expanded); path alphabet closed and values URL-encoded; header name validated, header value never described or logged. |
| Hardening | **Circuit breaker** — 3 consecutive *provider-level* failures (transport/timeout/5xx; a **4xx never counts**, it is a message rejection, not an outage) open the circuit for 5 min, doubling per consecutive open, capped at 30 min. While open a flush makes zero requests, consumes zero attempts and leaves every entry `PENDING`, so an outage postpones notifications instead of exhausting them; half-open sends **exactly one** probe. Re-checked between entries, so the first flush of an outage costs 3 timeouts, not one per due entry. **Fails closed** (a corrupt state file reads as "attempt delivery") and cannot silently suppress. Kill switch `MYTHOS_BRIDGE_WHATSAPP_BREAKER=off`; operator repair `mythos-github-bridge notify-breaker-reset` (sends nothing). **Flush budget** — `FLUSH_BUDGET_MS` (60 s) bounds a whole flush, checked between entries *and* between recipients; a cut consumes no attempt and is reported as `deferred`, never `failed`. Two correctness rules go with it: `SENT` now means *every* recipient is in `delivered_to` (deriving it from "no failure this attempt" would have marked a budget-cut entry delivered — a latent bug the budget would have made reachable), and a budget cut rolls back its own attempt increment. **Readiness split** — queue scope (provider, gateway address + private-network fence, instance, recipients, adapter config: all static, all still refuse to queue) vs delivery scope (**the credential only**, re-read on every flush). |
| Untouched by design | The two-phase design, the ledger and its `(task_id, kind)` key, `O_EXCL` locking, the at-least-once guarantee and its documented window, redaction, the private-network fence, recipient/instance validation, `SENT`-never-pruned retention, the customer-chat scope fence, `providers/evolution.js`, `http-json.js`, and every `github-bridge.js` call site. No governance-protected path, no `control/`, no unit file, no credential in Git, no deployment, no push. |
| Tests | `tests/mythos-bridge-whatsapp-resilience-test.js` **new, 95/0** (stable over 3 consecutive runs), offline against a real local HTTP recorder with the real adapters: multi-adapter contract conformance, generic-vs-evolution byte equivalence, template-injection refusal, end-to-end delivery through a **non-default** provider, the full breaker lifecycle incl. 4xx-does-not-open / corrupt-file-fails-closed / probe / reset, the budget incl. "every recipient gets exactly one message across both flushes", credential-loss survival, the private-host fence, and the F2 regression guard. `tests/mythos-bridge-whatsapp-notify-test.js` **131/0** (two edits: the breaker is `off` for that suite because sections 7–9 deliberately drive repeated failures, and the "exactly one provider" assertion was replaced by the selected-by-name property). Regression suites green on this tree: `mythos-github-bridge-test.js` 150/0 · `mythos-github-issues-test.js` 193/0 · `mythos-ai-executor-test.js` 390/0 · `mythos-governance-invariant-test.js` 111/0 · `bridge-action-resolution-test.js` 88/0 · `mythos-unattended-policy-test.js` 53/0 · `mythos-bridge-push-guard-test.js` 23/0. |
| Not done / not proven | **No gateway deployed, no real WhatsApp message sent** — end-to-end WhatsApp delivery remains unproven on this host; everything up to the socket is proven against a real HTTP server. **`wa-evolution` was not examined at all** — it needs a networked session. The F2 fix is guarded at *source level* (the Issues path needs a GitHub token and network to run), which prevents the branch from losing the call again but does not prove a live Issues tick delivers. |
| VPS reality (measured 2026-09-03 21:06 UTC) | 7746 MiB total / **1781 MiB available**, **swap 4095/4095 fully consumed**, PSI memory `full avg60=0.00` (no live stall — the swap is accumulated history), 4 vCPU, `/` 83 % full. The deploy user **can** now reach the Docker daemon (it could not on 2026-09-02), so **resources, not permissions, are the decisive constraint**. Verdict: do **not** deploy any gateway until the swap exhaustion is resolved (gh-issue-144 session guard is written but **not installed**), then measure a candidate under a hard `--memory` cap for a week. |
| Docs | New `docs/MYTHOS_WHATSAPP_PROVIDER_STRATEGY.md` (root-cause split, measured resource reality, candidate evaluation with the unverified parts marked as unverified, the decision, the delivered hardening, and the 7-step gate before any provider is deployed or replaced). `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md` updated (§3.6 generic adapter, §4 config table, §4.2 the F2 defect, §6.1–6.3, §7 CLI, §8 both suites, §10 risks). Open Source Registry record updated: verdict unchanged, `wa-evolution` recorded as still unverified. |
| Next stage | Owner decision, in this order: 1) review and merge this branch; 2) install the gh-issue-144 session guard and re-measure swap; 3) a **networked** session to live-verify `wa-evolution` / WAHA per §6 of the strategy doc; 4) only then deploy a gateway, drive it through `generic` against a local recorder, and run `notify-test --confirm` once. No auto-merge, no automatic production deployment. |

## EXEC-LIFECYCLE-0 — MYTHOS Execution Lifecycle: task / execution / session correlated, VPS + PC (2026-09-04)

**Objective.** Stop treating "a `ccd-cli` process exists" as "a Claude session is working" and "a GitHub
report exists" as "the session closed". Make MYTHOS able to state, for any unit of work, the TASK state
(GitHub truth), the EXECUTION state (who/where/attempt), the SESSION state (is the agent process alive,
mid-turn or idle) and the GitHub evidence — linked `Issue → control task → execution_id → agent → session_id
→ PID` — and turn the Session Guard from a process counter into a lifecycle-aware guard, for Claude on the
VPS and Claude on a PC under one model. Design: `docs/MYTHOS_EXECUTION_LIFECYCLE.md`; guard delta:
`docs/MYTHOS_SESSION_GUARD.md` §11.

**Measured before changing anything (read-only, 2026-09-04 08:0x UTC).** 18 agent-class processes: 14
Desktop Remote sessions whose transcripts ended with `end_turn` days ago (2.5 GiB), 1 ACTIVE (this
session), 3 UNKNOWN (executor daemon + helpers). The gh-issue-144 guard (installed, observe mode) vetoed all
14 as `recent_activity` on every tick: an idle `ccd-cli` burns ~2.5 % CPU so the CPU-tick idle clock never
clears. Claude Code keeps its own per-pid registry (`~/.claude/sessions/<pid>.json`: sessionId, procStart,
cwd) and the transcript's last main-line record says running (`tool_use`) vs idle (`end_turn`); the
installed ccd-cli 2.1.260 carries SessionStart/Stop/SubagentStop/TaskCompleted/SessionEnd hooks.

| Item | State |
|---|---|
| Branch | `mythos/execution-lifecycle-20260904` over `5482db8` (`origin/main`), worktree `/home/deploy/worktrees/execution-lifecycle`. Delivery by the governance relay; **not merged, no auto-merge**. |
| New | `projects/mythos-ai-executor/lib/lifecycle/{model,registry,runtime-vps,runtime-pc,correlate,verify,cleanup,index}.js`; `bin/mythos-lifecycle`; `ops/lifecycle/{claude-lifecycle-hook.js,claude-settings-hooks.example.json,mythos-pc-agent.js,install-lifecycle-hooks.sh,README.md}`; `docs/MYTHOS_EXECUTION_LIFECYCLE.md`; `tests/mythos-lifecycle-test.js` |
| Changed | `executor.js` (emits EXECUTION_CREATED/DISPATCHED, SESSION_STARTED with pid+start ticks, SESSION_END with process-gone proof, TASK_COMPLETED, REPORT_SUBMITTED for non-bridge tasks, EXECUTION_FAILED; `lifecycle.tick()` in the daemon step; `lifecycleStatus()`), `bridge/github-bridge.js` (task link at claim: issue/control id/OTHMODE id; REPORT_SUBMITTED in `finishTask`), `server.js` (`GET /lifecycle`, `/lifecycle/host`, `/lifecycle/{executions,sessions,tasks}/<id>`; `POST /lifecycle/events` bearer+HMAC relay ingest, evidence-only; `GET/POST /lifecycle/outbox/PC[/ack]`), `lib/session-guard.js` (registry consult: `lifecycle_execution_active` / `lifecycle_human_review` fences, rule 2b `lifecycle_close_requested`; transcript-turn idle clock from the host snapshot), `ops/session-guard/mythos-session-guard-run.js` (snapshot before plan, written to `/var/lib/mythos/lifecycle/host-sessions.json`), `.service` (`ReadWritePaths=/var/lib/mythos/lifecycle`), `install-session-guard.sh` (installs `runtime-vps.js`, creates the directory), `tests/session-guard-test.js` (runner dependency contract now allows the optional sibling), `docs/MYTHOS_SESSION_GUARD.md` §11 |
| Untouched | the task state machine (`lib/state.js`), the bridge's control protocol and schemas, OTHMODE, the Issues adapter, providers, the executor HTTP contract for existing routes. Every lifecycle call in executor/bridge is best-effort (`emit()` never throws). |
| Tests | `tests/mythos-lifecycle-test.js` **254/0** (25 scenarios + executor/bridge/HTTP/hook/runner/PC/CLI integrations; synthetic /proc + synthetic `~/.claude`; injected killFn — no real signal). Unchanged suites green in the worktree: executor 390/0, session-guard 277/0, github-bridge 150/0, github-issues 193/0, action-resolution 88/0, whatsapp-notify 131/0, push-guard 23/0, bridge-timer 16/0, resource-guard 91/0. Full suite not rerun (unrelated modules untouched). |
| Live proof (read-only) | `mythos-lifecycle host` on this VPS: 14 IDLE / 1 ACTIVE / 3 UNKNOWN, all bound pid↔uuid with verified start ticks. Updated root runner in observe mode with scratch state: `turn_idle_seconds` 2 586–177 309 s on 14 sessions, plan = SIGTERM for 3 (`idle_source: transcript_turn`), running session effective idle 0, **nothing applied**. |
| Installed / enabled | **Nothing.** Hooks not wired, registry directory not created for production, guard runner not re-installed, cleanup marker absent, no relay secret. The executor daemon will start emitting into `~deploy/mythos-ai-executor/lifecycle/` after a merge + restart (best-effort, dry-run cleanup). |

**How it works, in one screen.** Executor/bridge/hooks emit events → `registry.ingest` (idempotent, atomic,
ledgered) → reducer keeps TASK / EXECUTION / SESSION separate → `REPORT_SUBMITTED` puts the execution in
VERIFYING and `verify.js` *looks* at the session through its runtime (VPS: `/proc` identity + Claude's
`sessions/<pid>.json` + transcript turn; PC: relayed state aged by heartbeat) → `COMPLETED + SESSION_OPEN`
is a first-class state re-checked with 1m/2m/5m/15m/30m/1h backoff → `cleanup.js` walks OBSERVE → ELIGIBLE
→ GRACE → CLOSE_REQUESTED → VERIFYING → CLOSED only for linked, reported, non-active, transcript-idle
sessions (dry-run until `cleanup.enabled`; force kill off by policy; failure → HUMAN_REVIEW) → the root
Session Guard, which alone holds CAP_KILL, applies SIGTERM for `CLOSE_REQUESTED` sessions and uses the
transcript-turn clock for unlinked ones, under all its existing fences. `recover()` turns a vanished pid
into UNKNOWN (never FAILED), catches up from the executor's record, and marks superseded attempts.

**Owner steps (in order).** 1 `sudo bash ops/lifecycle/install-lifecycle-hooks.sh` · 2 `sudo bash
ops/session-guard/install-session-guard.sh` (re-installs runner + `runtime-vps.js`, unit change) · 3 as deploy:
`systemctl --user restart mythos-ai-executor` after the merge · 4 watch `mythos-lifecycle status|host|cleanup plan`
and `journalctl -u mythos-session-guard` · 5 `touch ~deploy/mythos-ai-executor/lifecycle/cleanup.enabled` and the
guard's own marker when satisfied · optional PC agent per `ops/lifecycle/README.md`. Rollbacks: remove the markers,
`MYTHOS_LIFECYCLE_CLEANUP=off`, `/usr/local/lib/mythos-lifecycle/unwire.js`, or revert the commit.

**Not done / limits.** No hook is live yet, so today's Desktop Remote sessions are correlated only via
Claude's own registry + transcripts (host view / snapshot) and not linked to executions; the PC agent is a
reference implementation exercised offline; `gh` is not installed on the host so the PR is an owner step
(command in the final report); the Issues adapter's comment is not a separate report event.

**Next stage.** Owner review + merge → install steps 1–3 → 2–3 days of observe → enable cleanup, then the
guard marker → PC agent pilot behind an SSH tunnel with `allow_close:false` first.

## gh-issue-142 — gh-issue-128 reconciled by evidence: the governed READ path is live (2026-09-03)

**Objective (GitHub Issue #142).** Finish whatever remained of #128 — but only after checking whether HOSTOPS-2R
(#130) and HOSTOPS-2R-FIX (#132) already closed it, instead of re-running work already done.

| Item | State |
|---|---|
| Prior state | #128 (attempt 1) reported **BLOCKED**: the required single live `POST /hostops/run {operation:health}` call returned `HOSTOPS_UNAVAILABLE` because the HOSTOPS-1 sudo boundary can never work under the executor's `NoNewPrivileges=true`. #130 (HOSTOPS-2R, `8c8ab41`, **merged to main**, confirmed ancestor of current `origin/main` `ff9f71b`) fixed the design with a root socket-activated daemon outside the executor's process tree. #132 (HOSTOPS-2R-FIX, `eebb4b1`, delivered on `mythos/gh/gh-issue-132`, **not yet merged to main**) fixed the one remaining production gap: `usermod -aG` alone never refreshes an already-running `systemd --user` manager's groups, so `deploy`'s manager needed `user@<uid>.service` restarted before the socket was reachable. |
| Live re-verification (this task) | `id deploy` on the live host now shows group `mythos-hostops` (1003) present — the group-refresh gap #132 diagnosed is already applied on this host. One live `POST /hostops/run {operation:health}` call was made through the running `mythos-ai-executor.service` (127.0.0.1:8130, bearer token read locally from the 0600 `executor.env`, never printed or persisted): **`ok:true`, `operation: host.health.check`, `class: READ`, `audit_id: hostops-mtlx0zuw-284786`, `hostops_exit: 0`**. This is the exact deliverable #128 asked for, now proven against the code currently on `origin/main` plus the host's already-applied group-refresh state. No WRITE/RESTART/DEPLOY verb was invoked; class-based refusal of those verbs is structurally enforced and covered by the passing suites below. |
| Reconciliation | **#128 is evidence-closable.** Nothing needed re-implementing — the fix already exists on `origin/main` (#130) and the one missing production-activation step (#132's group refresh) is already in effect on the live host, even though #132's branch itself is not yet merged. Merging #132 to main is an owner decision, unchanged by this task. |
| Tests (as deploy, this worktree, no code changed) | `tests/mythos-hostops-executor-test.js` **36/0** · `tests/mythos-hostops-test.js` **39/0, 2 skipped** · `tests/mythos-hostops-daemon-test.js` **14/0** · `tests/dagu-hostops-allowlist-test.js` **7/0**. |
| Files changed | `docs/AI_HANDOVER.md` only (this entry). No code, no `control/`, no protected path. |
| Owner action still open | Merge `mythos/gh/gh-issue-132` to main so the group-refresh script and its docs are not left stranded on a branch while already active in production. |
## HOSTOPS-2R-FIX — the group-membership activation gap closed (GitHub issue #132, 2026-09-03)

**Objective.** HOSTOPS-2R's own activation instructions said `deploy`'s new
`mythos-hostops` group membership "needs a fresh login session (or restart the executor's
user manager)" — that turned out to be the actual production blocker. Live symptoms:
`deploy` was already listed in `getent group mythos-hostops`, the socket was `0660
root:mythos-hostops` and listening, `mythos-ai-executor.service` had been restarted on the
new code, yet `/hostops/run` still hit `EACCES` on `/run/mythos-hostops/hostops.sock`. A
manual `SupplementaryGroups=mythos-hostops` drop-in on the executor unit made it worse:
`status=216/GROUP` (systemd.exec(5): that directive has no effect on `--user` units, whose
manager lacks `CAP_SETGID`). That drop-in was removed before this fix; it is never
reintroduced.

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-132` over `85cbf90` (`origin/main`). Delivery by the governance relay; **not merged**. |
| Root cause | `usermod -aG mythos-hostops deploy` edits `/etc/group` only — it does not update the supplementary groups of any already-running process, including `user@<uid>.service` (the root system unit that forks `deploy`'s `systemd --user` manager and everything under it). Restarting `mythos-ai-executor.service` alone restarts a *child* of that still-stale manager, so it inherits the old group list, not a fresh read of `/etc/group`. |
| Fix | New `ops/hostops/refresh-group-membership.sh`, called by `install-hostops.sh` right after the `usermod -aG` calls. Restarts `user@<uid>.service` (root, system-unit territory) for any user whose manager is already active — a fresh `systemd --user` fork re-reads `/etc/group` at that moment. Idempotent/non-disruptive: a user with no active manager (fresh install, or `dagu`, a nologin system account) is simply skipped, not an error. No socket unit, daemon, helper, or adapter code touched; `NoNewPrivileges=true` on the executor untouched. |
| Tests | `tests/mythos-hostops-group-refresh-test.js` (new, **10/0**): the script run against a stub `systemctl`/`id` (no root/systemd needed) proves an active manager is restarted and an inactive one is left alone; asserts no unit file in the tree declares `SupplementaryGroups=`; asserts `NoNewPrivileges=true` is unchanged; asserts the installer wires the refresh in after group grant, for both `deploy` and `dagu`. All prior HostOps suites re-verified green: daemon 14/0, executor adapter 36/0. |
| Owner activation | Re-run `sudo bash ops/hostops/install-hostops.sh` on the already-installed host — idempotent, now includes the group refresh, so no logout/login/reboot is required. Then `systemctl --user restart mythos-ai-executor` (as `deploy`) and verify per `docs/MYTHOS_HOSTOPS_INTERFACE.md` HOSTOPS-2R-FIX addendum. |
| Not done (by session) | No root install performed from this execution session — the installer/systemd/group changes are owner-only per this repository's rules, and this session cannot touch the live VPS. |

## gh-issue-148 — Delivery/Reconciliation for #132 and #142: both PR-ready, neither merged (GitHub issue #148, 2026-09-04)

**Objective.** Reconcile #132 (HOSTOPS-2R-FIX) and #142 (HOSTOPS read-path final verification) against
work already done, without re-implementing or re-running proven work — identify exactly what is
implemented, what is pushed, and what only remains for delivery (PR / reconciliation).

| Task | Existing implementation | Evidence | Commit | Remote | PR | Action |
|---|---|---|---|---|---|---|
| #132 | `ops/hostops/refresh-group-membership.sh` + installer wiring, fixes the systemd `--user` manager group-refresh gap left by HOSTOPS-2R (#130, already on `main`) | `tests/mythos-hostops-group-refresh-test.js` 10/0 (per branch's own handover entry; asserts `NoNewPrivileges=true` unchanged and no `SupplementaryGroups=` anywhere) | `eebb4b1` on `mythos/gh/gh-issue-132` (base `85cbf90`) | pushed to `origin` | none open | **PR_READY** |
| #142 | Docs-only reconciliation: #128's remaining requirement (one live governed READ call) is satisfied by evidence, not re-implementation | Recorded in-branch: `ok:true`, `class: READ`, `audit_id: hostops-mtlx0zuw-284786`, `hostops_exit: 0`, no WRITE/RESTART/DEPLOY verb invoked | `ad67ab6` on `mythos/gh/gh-issue-142` (base `ff9f71b`) | pushed to `origin` | none open | **PR_READY** |

| Item | State |
|---|---|
| Method | Git-only reconciliation, no code re-implementation, no E2E rerun (this task's own hard prohibitions). `git ls-remote origin refs/pull/*/head` shows no open PR head at any commit in either branch's history. `git merge-base` + `git merge-tree` of both branches against current `origin/main` (`5482db8`) show a **clean auto-merge on every code and doc file** except one trivial, non-overlapping `docs/AI_HANDOVER.md` append-point conflict each (both branches, like this one, prepend a new section at the same line) — resolvable by keeping both sections, not a real conflict. |
| #132 security check | Read the full diff and test file: no `sudo`/`docker` bypass introduced, the script only restarts `user@<uid>.service` (a root system unit, not a hostops verb) to force a fresh `/etc/group` read, `NoNewPrivileges=true` on the executor is asserted unchanged, and the earlier `SupplementaryGroups=` (`216/GROUP`) workaround is asserted absent from every unit file in the tree. No WRITE/RESTART/DEPLOY path added. |
| #142 evidence check | The branch's own commit message and `docs/AI_HANDOVER.md` entry already record the exact deliverable #142/#128 required — a live `POST /hostops/run {operation:health}` returning `ok:true`, `class: READ`, a non-empty `audit_id`, `hostops_exit: 0` — through the code already on `origin/main` (`#130`, `8c8ab41`, confirmed ancestor) plus #132's not-yet-merged fix, which the live host already carries per that entry. Nothing here needed re-running. |
| Gap | Both branches are pushed and complete, but **not merged to `main`** and **no PR is open for either** (verified, not assumed). Opening the PR requires no new push (commits already on `origin`) — only `gh pr create` (or the GitHub UI), which is not something this session performed: it is an owner/relay step in the delivery flow (`… → Governance/Relay → PR → Human Review → Human Merge`), and this sandboxed autonomous run has no approved path to call the GitHub API or `gh` CLI directly. |
| Recommended PR (owner/relay) | #132: `gh pr create --base main --head mythos/gh/gh-issue-132 --title "fix(hostops): HOSTOPS-2R-FIX — refresh deploy/dagu systemd --user manager (gh-issue-132)"`. #142: `gh pr create --base main --head mythos/gh/gh-issue-142 --title "docs(hostops): reconcile gh-issue-128 against HOSTOPS-2R/2R-FIX (gh-issue-142)"`. Both will show the same one-line `docs/AI_HANDOVER.md` conflict on GitHub, trivially resolved by the human merger keeping both prepended sections; no other file conflicts. |
| Not done (by design) | No re-implementation of #132 or #142. No E2E rerun. No manual VPS action. No change to WhatsApp, Status Center (#140), Resource Guard thresholds, MCP/Governance architecture, or `control/`. No `git add .`. No auto-merge. No PR actually created (blocked, see above — reported as the delivery gap, not silently skipped). |
| Files changed (this task) | `docs/AI_HANDOVER.md` only, on `mythos/gh/gh-issue-148`. |
| Next stage | Owner or relay opens the two PRs above against `main` (`gh pr create`); human review; human merge. Nothing else is pending on #132 or #142 beyond that. |

## SESSION-GUARD-1 — Claude Desktop Remote session lifecycle guard (GitHub issue #144, 2026-09-03)

**Objective.** Stop the unbounded accumulation of Claude Desktop Remote sessions on this VPS and
protect production memory, permanently and under governance. Root cause confirmed by direct `/proc`
capture, not inferred: `/root/.claude/remote/srv/<rev>/server --serve` forks one `ccd-cli` process
per Desktop Remote session and never reaps it — 47 started since 2026-08-30, **14 still resident
holding ~2.6 GiB**, some days old, all root-owned in `user-0.slice`, while production runs under
`user-1001.slice`. That server has no idle timeout and no concurrency ceiling and is not ours to
change, so reclamation from outside is the only lever. **Swap at 96–100 % is a historical artefact
of accumulated pages, not the root cause** — the cause is process accumulation, which is what this
bounds.

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-144` over `ff9f71b` (`origin/main`), worktree `/home/deploy/mythos-ai-executor/worktrees/gh/gh-issue-144`. Delivery by the governance relay; **not merged**. |
| Guard | New `projects/mythos-ai-executor/lib/session-guard.js`. Classifies every process into `executor` / `remote-server` / `remote-session` / `other`, with the **executor test first** so an ambiguous argv is protected, never reclaimed. Sessions keyed by `pid:starttime`, and identity re-verified from `/proc` immediately before every signal, so a recycled PID is aborted rather than killed. Four states (`active`/`idle`/`orphaned`/`terminating`) driven by three ORed activity signals: CPU ticks, RSS movement ≥ 8 MiB, and `<session_ref>.jsonl` transcript mtime (root-only, degrades to `null`, never to a wrong answer). Four rules in order — `sigterm_ignored` (SIGKILL after a 120 s grace), `orphaned` (5 min settle), `idle_timeout` (1 h; 15 min under memory pressure), `concurrency_limit` (10 min floor) — each declaring its own required inactivity. Eleven fences veto a signal and every veto is written to the ledger. Blast radius: 3 terminations per run, the excess deferred and recorded. SIGTERM always first. **Fail-closed** (the opposite of the Resource Guard): no evidence, no signal. |
| Concurrency | Default ceiling 6, hard cap 8 that **cannot be configured upward** (a larger value is clamped and the clamp reported). Enforced by *reclamation*, not admission — the remote server exposes no admission hook, so `admission()` is returned explicitly flagged `advisory: true`. A breach that cannot be resolved without touching a working session is reported as `unreclaimable` and left alone. |
| Memory | Three layers, none of which can OOM-kill: the pressure-lowered idle threshold; `ops/session-guard/user-0.slice.d/memory.conf` (`MemoryHigh=2G` soft throttle on the root login slice — **`MemoryMax` deliberately not set**, a hard cap there would OOM-kill at an arbitrary moment); and the guard's own unit capped at `MemoryMax=192M` / `CPUQuota=25%` / `OOMScoreAdjust=500`. The memory level is **read from the existing Resource Guard** (gh-issue-101) and never recomputed; a state file older than 5 minutes is ignored and read as `NORMAL`. |
| Observability | CLI `projects/mythos-ai-executor/bin/mythos-session-guard` (`inventory`, `status`, `plan`, `observe`, `enforce --yes`, `ledger`). Durable JSONL ledger `session-guard.jsonl` (`session_seen`, `session_state`, `session_exited` with `terminated_by_guard` + reason, `terminate_signalled` / `_aborted` / `_failed` / `_vetoed`), each line carrying pid, session uuid, age, idle seconds, RSS and the deciding evidence. New **read-only** `GET /session-guard` on the executor API (`executor.sessionGuardStatus()` → `snapshot()`, which writes nothing and signals nothing; there is no `POST`). Full behaviour doc: `docs/MYTHOS_SESSION_GUARD.md`; operator runbook: `ops/session-guard/README.md`. |
| Rollout | **Nothing was installed and no session was killed by this task**, as the issue requires. `ops/session-guard/install-session-guard.sh` (owner action, root) copies exactly two root-owned files to `/usr/local/lib/mythos-session-guard` — root must never execute code from the deploy-writable checkout — and enables a 5-minute timer that runs in **observe mode**: it tracks, plans and logs, and signals nothing. Enforcement is a separate explicit act (`touch /var/lib/mythos-session-guard/session-guard.enabled`); rollback is `rm` on that file; hard off is `MYTHOS_SESSION_GUARD=off` or disabling the timer. `enforce --yes` without the marker is *not* an error — it runs the full cycle with enforcement off, which is what makes the timer safe to install before the decision to enforce is taken. |
| Tests | `tests/session-guard-test.js` **274/0**, offline and deterministic: every enforcement test injects a `killFn` that records instead of signalling, and the scanner runs over synthetic `/proc` trees. Covers `/proc` parsing (hostile `comm`, and the USER_HZ regression that made an unresolved config produce a `NaN` age that read as "old enough"), executor-precedence classification, PID-reuse, all three activity signals, every rule and every fence, the blast-radius cap, SIGTERM→SIGKILL escalation, identity re-verification aborting a recycled/vanished/executor target, kill switch and enable marker, fail-closed on unreadable `/proc`/`stat`/corrupt state, persistence, the non-mutating `snapshot()`, the systemd artifacts, the root runner end-to-end in the installed layout, and the CLI (including that `enforce` without `--yes` refuses with exit 3). Classification regression runs against the **real** capture `tests/fixtures/session-guard/host-20260903.json` (argv truncated to 280 chars, every UUID replaced with a placeholder, the server's `--token-file` name redacted): 14 sessions + 2 servers + the deploy-owned executor processes, each classified as recorded, >2 GiB held, over the ceiling by 8, and zero actions on first observation. Unaffected suites re-run green on this tree: `mythos-ai-executor-test.js` 390/0, `resource-guard-test.js` 91/0, `mythos-github-bridge-test.js` 150/0, `mythos-governance-invariant-test.js` 111/0, `mos-1-console-test.js` 1438/0, `mythos-hostops-test.js` 39/0/2 skipped. |
| Not done (by design) | No install performed (root systemd + `/usr/local` are owner-only). **No existing session terminated** — the issue forbids it before guard, tests and rollback exist, and all three now do. No change to the Resource Guard, Governance, the Bridge, OTHMODE or any production service. No `git add .`; no auto-merge. `ops/oom/` untouched. |
| Separate note (not this task's scope) | The executor's `Failed at step GROUP` journal note is **not** part of this lifecycle problem: those sessions are root-owned children of the Desktop Remote server, entirely outside `mythos-ai-executor.service`. Recorded here only so it is not lost; it needs its own issue. |
| Owner activation (in order) | 1) merge this branch through review; 2) `sudo bash ops/session-guard/install-session-guard.sh`; 3) watch `journalctl -u mythos-session-guard.service -f` and `/var/lib/mythos-session-guard/session-guard.jsonl` for several hours in observe mode and confirm the planned targets and veto reasons look right for this host; 4) `touch /var/lib/mythos-session-guard/session-guard.enabled`; 5) optionally, as a separate decision, install `ops/session-guard/user-0.slice.d/memory.conf`. |


## gh-issue-140 — Status Center recuration completing the gh-issue-95 audit (2026-09-03)

**Objective (GitHub Issue #140).** The gh-issue-95 audit had found the Status Center's registry stale and its
discovery path blind to this repository's own `projects/*` directories, but never implemented a fix — only
investigated. This stage does the actual implementation: GitHub-verified evidence only, no invented percentage
or status, "smallest coherent change" over "rebuild what already works" (`lib/engine.js`/`lib/model.js`/the site UI
were sound and are extended, not replaced).

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-140` over `ff9f71b` (`origin/main`). Delivery is the governance relay (`mythos-git-push.timer`, fast-forward only); **never pushed directly, never merged to main.** |
| Stale entry fixed | `PROJECT-AI-ORCHESTRATION` had read "v1 COMPLETE AND CLOSED 2026-08-19" for ten days and roughly a dozen delivered bridge/executor stages (GitHub Issues intake, model-selection policy, Resource Guard, bridge retry/quota policy, the HostOps read-only boundary — HOSTOPS-2R above, all real, all tested, all on unmerged branches) without the registry entry moving at all. It now states both facts plainly: v1 is still closed as scoped (unchanged), AND the same component has kept evolving since (new evidence `EV-AI-ORCH-EVOLUTION-0903`, plus two independently git-verifiable merged commits `EV-BRIDGE-PUSH-GUARD-COMMIT` `b9a1e6c` and `EV-BRIDGE-TIMER-ONESHOT-COMMIT` `cc41fd7`). No percentage changed — a new `post_v1_evolution` dimension carries a status only (`IN_PROGRESS`), not an invented number. |
| Missing projects added | Listed every `projects/*` directory (21) and checked which ones no existing project's purpose/evidence text actually covered: 17 already did (confirmed against `git log` and each project's own purpose text — e.g. `mythos-core` is the ID Auto contracts boundary, `personal-intelligence` is `PROJECT-OTH-MASTER`, `meta` supplies `PROJECT-DEVX`'s stage-runner/ledger data). 4 did not: **`mythos-gateway`, `mythos-orchestrator`, `mythos-vault`, `oth-mcp`** — real, tested, partially-deployed components with zero registry entry. Added as `PROJECT-MYTHOS-GATEWAY`, `PROJECT-MYTHOS-ORCHESTRATOR`, `PROJECT-MYTHOS-VAULT`, `PROJECT-OTH-MCP`, each cited against `docs/MYTHOS_MCP_ECOSYSTEM.md` (the existing, detailed MCP-ECOSYSTEM-1 record) and a freshly re-run test suite, not an assumed one. Every project (old and new) now also carries a `directories` field recording which `projects/*` path(s) it accounts for, making the mapping explicit instead of implicit. |
| Monorepo discovery added | `discoverRepositories` only ever compared GitHub account repositories — structurally blind to a new directory added under this repo's own `projects/`. New `lib/engine.js` `discoverMonorepoProjects(repoRoot, registryProjects)`: lists `projects/*` on disk (fs only, no git, no network), diffs against the union of every project's `directories`, and reports any unclaimed directory as a discovery requiring classification — the same non-silent guarantee `discoverRepositories` already gives GitHub repos. Wired into `runReview` as `snapshot.monorepo_discovery` and printed by `bin/review.js`. Against the real repository right now: 21 directories checked, 0 unclaimed. `lib/model.js` gained matching validation for the new `directories` field (must be an array of `"projects/..."` strings). |
| Simplify-display assessment | Inspected `assets/app.js` before changing anything: `curation_note` (the multi-paragraph internal curator log) is **never rendered** on the site at all, and every per-evidence `note` is shown only as a hover `title` attribute on a small chip, never dumped inline. The "hide old technical detail, keep operational info" requirement is therefore already substantially met by the existing design; no UI rebuild was made without evidence of a problem. The one concrete UI change is additive: Arabic one-line labels for the 4 new projects, following the exact existing `AR.projects` convention (missing keys already degrade gracefully to no Arabic line, never a crash — verified by `stc-ar` before and after). |
| Review run for real | `node projects/status-center/bin/review.js` executed (not a dry run): **REVIEW-2026-09-03-001**, 27 projects (was 23), 109 evidence items (77 verified here, 32 RECORDED, **0 NOT_VERIFIED**), 0 new monorepo discoveries, 1 pre-existing unclassified GitHub-repo discovery (`othoth77/spy`, carried over from the 2026-09-02 refresh, still requires an owner classification decision — not something this environment can resolve without a network-verified look at that repo's content). Published to `sites/status.mythosprod.xyz/data/current.json`, `reviews-index.json` (23 snapshots), `health.json`, and the new immutable `reviews/2026/2026-09-03-review-001.json`. Verified after writing: `current.json` carries all 4 new projects and the corrected `PROJECT-AI-ORCHESTRATION.last_change`; `health.json` `repository_head` matches `git rev-parse HEAD`. |
| Tests | `tests/stc-1-status-center-test.js` **81/0** (was 73; +8 assertions for `discoverMonorepoProjects` and the `directories` validation) · `tests/stc-2-monitor-test.js` **86/0** (unchanged, confirms monitor/live-panel untouched) · `tests/stc-ar-arabic-layer-test.js` **50/0** (before and after the 4 new Arabic labels) · `tests/gateway-boundary-test.js` **37/0** (re-run fresh) · `tests/othk-6-mcp-server-test.js` **58/0** (re-run fresh) · `tests/mcp-ecosystem-test.js` **168/0** (re-run fresh, covers the Vault inventory stat-only check) · `node --check` on every changed `.js` · registry `JSON.parse` + `model.validateRegistry` clean (0 errors). `tests/mythos-orchestrator-0-test.js` (156/0) and `tests/mythos-orchestration-core-test.js` (255/2, the 2 being documented pre-existing VPS-only systemd checks) were **not** re-run — attempted, exceeded this sandbox's command timeout; reused as a prior recorded result since `projects/mythos-orchestrator/` has no commit since 2026-08-15 (AGENTS.md §8: reuse only when the tested code and base commit are unchanged). |
| Not done, honestly | No live VPS/HTTPS re-verification of `status.mythosprod.xyz` from this environment (no network egress here); publication was verified locally against the repository's own site files only, exactly like the 2026-09-02 discovery-refresh entry before it. The `othoth77/spy` discovery stays unclassified. No GitHub API refresh of `repo-snapshot.json` was attempted (network calls are gated in this sandbox); it is unchanged since 2026-09-02 and still current as far as this session can tell. Deployment to the VPS is a separate, explicitly out-of-scope owner action (`scripts/deploy-status-center.sh`). |
| Files | changed: `projects/status-center/data/registry.json`, `lib/engine.js`, `lib/model.js`, `bin/review.js`, `sites/status.mythosprod.xyz/assets/app.js`, `docs/ROADMAP.md`, this file; regenerated by the real review run: `sites/status.mythosprod.xyz/data/current.json`, `data/reviews-index.json`, `health.json`; new immutable: `sites/status.mythosprod.xyz/reviews/2026/2026-09-03-review-001.json`; test: `tests/stc-1-status-center-test.js`. No protected path touched — nothing under `control/`, no service/policy/budget file, no `.env`, no credential-shaped string (the Vault/MCP evidence added here is metadata/reference only, matching the existing `EV-MCP-ECOSYSTEM-DOC` convention). |
| Next stage | Owner: classify `othoth77/spy` in the registry; decide the accumulated unmerged branches named in `EV-AI-ORCH-EVOLUTION-0903`; when ready, deploy this review via `scripts/deploy-status-center.sh` to replace the stale live snapshot. |

## HOSTOPS-2R — Executor → HostOps fixed to a Unix socket boundary (GitHub issue #130, 2026-09-03)

**Objective.** HOSTOPS-1's `sudo -n mythos-hostops` boundary call, made from inside
`mythos-ai-executor.service`, was silently non-functional in production: that service runs
with `NoNewPrivileges=true` (load-bearing, never weakened), and under that flag the kernel
ignores the setuid bit on `exec()` for every child of the hardened process — `sudo` could
never actually reach root there, no matter what sudoers granted. Every real
`/hostops/run` call returned `HOSTOPS_UNAVAILABLE`. Fix the Executor→HostOps READ path
while keeping `NoNewPrivileges=true` exactly as-is.

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-130` over `718741b` (`origin/main`). Delivery by the governance relay; **not merged**. |
| Fix | Moved the privilege boundary out of the executor's process tree. New `ops/hostops/mythos-hostops.socket` + `ops/hostops/mythos-hostops.service` run a root daemon (`ops/hostops/mythos-hostops-daemon.py`, stdlib-only Python — the only stdlib with a `SO_PEERCRED` API without a native addon) started directly by systemd/PID 1, never a child of the executor, so the executor's `NoNewPrivileges=true` is completely untouched. Two independent identity gates: socket file permissions (`0660 root:mythos-hostops`, a new group containing `deploy` and `dagu`) and the daemon's own `SO_PEERCRED` check (kernel-reported uid, never a client-supplied field). The daemon invokes the SAME unmodified root-owned helper directly (`shell=False`, fixed argv, `SUDO_USER=<verified caller>` in a minimal replaced env) — the helper remains the sole allowlist/class/argument/audit authority, byte for byte unchanged. |
| Adapter | `projects/mythos-ai-executor/lib/hostops.js`: same governed order (closed fields → identity → allowlist → class READ → argument validation → Resource Guard admission, still before the socket is touched) with one swap — the boundary call is now a Unix socket round trip (`net.createConnection`) instead of `spawnSync(sudo, ...)`. `invoke()` now returns a **Promise** (inherently async); `server.js`'s `/hostops/run` route awaits it, same pattern as `mcpInvoke.invoke()`. Every existing failure code preserved: socket missing/refused → `HOSTOPS_UNAVAILABLE`, round-trip timeout (20s, unchanged) → `HOSTOPS_TIMEOUT`, helper exit 0 unparseable → `HOSTOPS_MALFORMED`, success without `audit_id` → `HOSTOPS_MALFORMED` (PR #127 hardening, unchanged), daemon-level peer-cred refusal → `HOSTOPS_CALLER_REFUSED` (new, same code the helper's own exit-3 already used for the same meaning). |
| Removed | `ops/hostops/61-deploy-hostops` (the sudoers fragment that never worked) deleted; `install-hostops.sh` no longer installs it. `60-dagu-hostops` (dagu's manual/owner verification path — not in the automated READ path, not a child of any hardened unit, was never broken by NoNewPrivileges) is untouched and still installed. |
| Installer | `ops/hostops/install-hostops.sh`: adds the `mythos-hostops` group (`deploy`, `dagu`), installs the daemon (0700 root:root) and both systemd units, `systemctl enable --now mythos-hostops.socket`. Full owner activation sequence: `docs/MYTHOS_HOSTOPS_INTERFACE.md` HOSTOPS-2R addendum. |
| Tests | `tests/mythos-hostops-daemon-test.js` (new, **14/0**): runs the REAL Python daemon against a temp socket + a stub helper — real SO_PEERCRED round trip with this process's own (deploy) uid, fixed-argv construction, malformed/oversized/injected requests refused before any subprocess spawns, no shell/docker/systemctl/sudo in the daemon (source-scanned), real end-to-end success and Resource-Guard-before-socket ordering through the actual (non-injected) `lib/hostops.js` client, plus a Python-level unit test of `resolve_caller()`/`validate_request()`/`build_argv()` proving an arbitrary uid is never trusted and a client-supplied identity field is refused. `tests/mythos-hostops-executor-test.js` rewritten for the async adapter, **36/0** (added: daemon-refusal mapping, socket-not-installed/connection-refused/empty-response unavailability, NoNewPrivileges-on-the-executor still asserted true, the new socket unit's permissions/group asserted, the deleted sudoers fragment asserted gone). Unaffected suites re-run green: `mythos-hostops-test.js` (helper itself, unchanged) 40/0/1 skip, `dagu-hostops-allowlist-test.js` 7/0, `mythos-ai-executor-test.js` 390/0, `mythos-github-bridge-test.js` 150/0, `bridge-action-resolution-test.js` 88/0, `model-selection-policy-test.js` 81/0, `mythos-governance-invariant-test.js` 111/0, `mythos-unattended-policy-test.js` 53/0, `mcp-ecosystem-test.js` 168/0, `resource-guard-test.js` 91/0. |
| Not done (by design) | No root install performed from this session (owner-only, systemd system units + group + sudoers are governance-protected); no change to Resource Guard, Governance, MCP, Model Selection or the GitHub Bridge; Dagu still not in the READ path and still loopback-only; no WRITE/RESTART/DEPLOY execution added anywhere. |
| Owner activation | See `docs/MYTHOS_HOSTOPS_INTERFACE.md` HOSTOPS-2R addendum §Activation — `sudo bash ops/hostops/install-hostops.sh`, then a fresh `deploy` session/executor restart for the new group membership to take effect, then verify via `/hostops/run` and `journalctl -u mythos-hostops`. Until installed, `/hostops/run` keeps returning the same tested `HOSTOPS_UNAVAILABLE` it already returns today — this change cannot make production behavior worse, only fix it once activated. |

## HOSTOPS-1 — Executor → mythos-hostops governed READ integration (2026-09-03 14:35–15:05 UTC)

**Objective.** First real MYTHOS integration of the installed READ-ONLY boundary: Executor → Governance →
Resource Guard → mythos-hostops, READ-only, no new authorization model, smallest wiring consistent with the
existing architecture. Contract + addendum: `docs/MYTHOS_HOSTOPS_INTERFACE.md`.

| Item | State |
|---|---|
| Branch | `mythos/hostops-executor-20260903` (worktree `/home/deploy/worktrees/hostops-executor`) over `3e4b765` (= `origin/main` 0ed68a1 + HOSTOPS-READONLY-0, whose install the owner completed and verified 40/0/1). Delivery by the governance relay; **not merged**. |
| Adapter | `projects/mythos-ai-executor/lib/hostops.js`: closed field set → identity validation → allowlist lookup (installed `/etc/mythos/hostops-allowlist.json`, repo fallback) → class READ enforced (WRITE/RESTART/DEPLOY refused by name with class, spawn never reached) → argument regex + metacharacter net + path normal form → **Resource Guard admission** (same options and fail-open posture as `guardGate()`; CRITICAL ⇒ deferred, recorded, nothing spawned) → `spawnSync /usr/bin/sudo -n /usr/local/sbin/mythos-hostops <verb> <flag array>` (no shell anywhere, no fallback of any kind) → single-JSON-body verification against the exit code → normalized outcome with `audit_id`, `hostops_exit`, `dagu_run_id: null`. Kill switch `MYTHOS_HOSTOPS=off`. |
| Wiring | `server.js`: `POST /hostops/run` + `GET /hostops/registry`, bearer-gated after `authorized()` exactly like `/mcp/invoke` — the only path from a task to the helper, because every enabled profile denies `Bash(sudo:*)` (asserted). Task record: `hostops_invoked` event + bounded task file `hostops.json` (never `status.json` — `transition()` stays the chokepoint). `github-bridge.js`: one additive field, `execution.hostops` from that file in the REPORT (join on `audit_id`); no semantics change. |
| Dagu decision | **Dagu is NOT in the READ path** (documented in the addendum + asserted: the adapter contains no Dagu/network client). One READ verb = one helper call; Dagu adds credential/lifecycle/latency and enforces nothing extra. Dagu stays the orchestration layer for future multi-step WRITE/DEPLOY workflows. PoC untouched, still loopback `127.0.0.1:8095`. |
| Helper v0.1.1 | `ops/hostops/mythos-hostops.js`: caller list `['dagu','deploy']` (the executor identity is the intended client); new `ops/hostops/61-deploy-hostops` sudoers fragment (deploy → exactly the helper, visudo-validated); installer updated to install both. Installed copy on the host is still v0.1.0 — reinstall is the owner activation step. |
| Tests | new `tests/mythos-hostops-executor-test.js` **29/0** as deploy (10 failure behaviours incl. guard-before-spawn ordering, governance-before-spawn, identity propagation, audit_id propagation, no-shell invariant, status.json untouched, root boundary intact, Dagu loopback). Existing suites on this tree (as deploy): hostops boundary **39/0** (2 env skips) · allowlist 7/0 · resource-guard 91/0 · executor 390/0 · github-bridge 150/0 · action-resolution 88/0 · model-selection 81/0 · governance-invariant 111/0 · unattended-policy 53/0 · mcp-ecosystem 168/0. Live boundary proof (installed v0.1.0 via `dagu`): `health` ok with audit id + task id; `file-write` refused `OPERATION_NOT_READ`. |
| Resource | before 2052 MiB avail / PSI 0.00 / oom 1323 → after ≈1990 MiB / PSI 0.00 / oom 1323; guard NORMAL throughout; Dagu PoC idle ~41 MiB; no Resource Guard threshold or policy touched. |
| Owner activation (in order) | 1) merge this branch through review; 2) `sudo bash ops/hostops/install-hostops.sh` from the merged checkout (helper v0.1.1 + `61-deploy-hostops`); 3) restart `mythos-ai-executor` (deploy user unit) so the daemon serves the routes; 4) verify: `curl -s -H "Authorization: Bearer $T" -X POST http://127.0.0.1:8130/hostops/run -d '{"operation":"health"}'` returns `ok:true` with an audit id, and `sudo -u deploy sudo -n /usr/local/sbin/mythos-hostops health` answers directly. |
| Not done (by design) | No WRITE/RESTART/DEPLOY, no approval consumption, no Evolution deploy, no Dagu client, no WhatsApp, no public exposure. |
| PR #127 review hardening (2026-09-03 15:15 UTC) | Two review findings fixed in `lib/hostops.js`: (1) a helper exit-0 `ok:true` **without a non-empty `audit_id`** is now refused as `HOSTOPS_MALFORMED` — a success that no ledger can trace is not a success; (2) `record()` returns null/true/false and every outcome carries `task_recorded`, so a failed or skipped executor-side record is visible on the result instead of silent (the root ledger still traces via audit_id). Sudo boundary audited: deploy's live grants are only the pre-existing scoped nginx/certbot rules; new tests pin the repo fragments to exactly the helper binary and assert no deploy rule ever names docker, a shell, node, sudoedit or unscoped systemctl. Adapter suite now **34/0**; all ten regression suites re-run green. |

## HOSTOPS-READONLY-0 — mythos-hostops v0.1 READ-ONLY security boundary (2026-09-03 14:05–14:25 UTC)

**Objective.** Implement and validate the root-owned boundary the Dagu assessment made a condition
(`docs/MYTHOS_DAGU_HOST_OPERATIONS.md` §7): Executor → mythos-hostops → READ-only host operations.
Six READ verbs only; no write, restart, deploy, shell, Docker mutation or production change.

| Item | State |
|---|---|
| Branch | `mythos/hostops-readonly-20260903` (worktree `/home/deploy/worktrees/hostops-readonly`) over `0ed68a1` (latest `origin/main`). Carries the DAGU-HOSTOPS-0 files verbatim from `96528d6` (allowlist, doc, ops/dagu-poc, its test) so it is self-contained; delivery by the governance relay; **not merged**. |
| Implementation | `ops/hostops/mythos-hostops.js` (→ `/usr/local/sbin/mythos-hostops`, 0700 root:root): verbs `health`, `docker-status`, `docker-logs`, `systemd-status`, `file-read`, `resource-guard` from `ops/dagu-poc/hostops-allowlist.json` (single authorization model — the helper adds no operation and executes READ class only, refusing WRITE/RESTART/DEPLOY verbs BY NAME with their class). No shell anywhere: `spawnSync` with fixed absolute binaries and argument arrays; anchored allowlist regex + metacharacter net + per-verb hard rules (file-read: normalized path, no `..`, realpath confined to `/home/deploy/{deployments,projects}`, regular files, size cap, secret-shaped basenames refused — including `*.env`, caught by the suite as a real bug and fixed). `resource-guard` verb OBSERVES only (reads /proc + the guard state file read-only, never samples/advances the machine). |
| Security model | caller gate: under sudo only `SUDO_USER=dagu` (exit 3); env overrides ignored under sudo; installed allowlist refused unless root-owned and not group/other-writable; audit JSONL per invocation (refusals included) in `/var/lib/mythos/hostops/`, **fail closed** — an unauditable success is withheld (exit 5). Sudoers: `ops/hostops/60-dagu-hostops` grants `dagu` exactly the helper binary, `visudo -cf` clean. Interface contract for the future executor `lib/hostops.js`: `docs/MYTHOS_HOSTOPS_INTERFACE.md` (request/authorization/validation/execution/result/audit/failure; exit-code → task-status mapping). No `POST /hostops/run` yet, by design. |
| Tests | `tests/mythos-hostops-test.js`: as root **39/0 (2 skip: fail-closed-audit not reproducible as root; install pending)**, as deploy **39/0 (2 skip: docker probe; install pending)** — combined, 14 of the 15 mission points proven, point 12 (installed ownership) waits for the owner install. Existing suites re-run as deploy on this tree: resource-guard **91/0** · executor **390/0** · github-bridge **150/0** · action-resolution **88/0** · model-selection **81/0** · governance-invariant **111/0** · unattended-policy **53/0** · mcp-ecosystem **168/0** · dagu-hostops-allowlist **7/0**. |
| Untouched | Resource Guard, Model Selection, MCP, GitHub Bridge, WhatsApp branch, Dagu PoC (still loopback 127.0.0.1:8095, basic auth), all production services; Docker socket exposed to nobody new. |
| **BLOCKED — owner root action** | The permission layer refused `bash ops/hostops/install-hostops.sh` (root install), as expected. Owner runs, from the repo checkout after fetching this branch: `sudo bash ops/hostops/install-hostops.sh` (creates the `dagu` system user; installs the helper 0700 root:root, the allowlist copy 0644 root:root at `/etc/mythos/hostops-allowlist.json`, the audit dir 0700, and `/etc/sudoers.d/60-dagu-hostops` 0440 after `visudo -cf`). Then verify: `sudo -u dagu sudo /usr/local/sbin/mythos-hostops health` and re-run `node tests/mythos-hostops-test.js` (point 12 turns green). |

## DAGU-HOSTOPS-0 — Dagu Host Control PoC and integration assessment (2026-09-03 13:20–13:45 UTC)

**Objective.** Determine whether Dagu can be the controlled host-operations layer under GitHub → Bridge → OTHMODE →
Executor, without replacing any of them and without giving Claude root. Inspect → research → isolated PoC → design.
Full record: `docs/MYTHOS_DAGU_HOST_OPERATIONS.md`.

| Item | State |
|---|---|
| Branch | `mythos/dagu-hostops-poc-20260903` (worktree `/home/deploy/worktrees/dagu-hostops`) over `0ed68a1` (`origin/main`). Docs + `ops/dagu-poc/` + one test only; no executor, bridge, OTHMODE, policy, guard, MCP or service file touched. Delivery by the governance relay; **not merged**. |
| Dagu | v2.16.2 (released 2026-09-02, `github.com/dagucloud/dagu`, sha256-verified tarball). Community mode: basic auth works; **audit logs, RBAC and API keys are Pro-licence** (verified against the running API). Approval steps, retry, logs, event log, MCP endpoint all work in community. |
| PoC | `/home/deploy/dagu-poc` (outside the repo), deploy-user unit `dagu-poc.service`, `127.0.0.1:8095`, basic auth, `write_dags: false`, `MemoryMax=256M`, `NoNewPrivileges`. Tests 1–11 PASS (start, read-only, Docker inspect, bounded container, systemd status, logs, failure+handler, retry 3rd-attempt success, graceful+SIGKILL recovery, MYTHOS health through Dagu, approval gate with required input). Idle 30–34 MiB, peak 149 MiB; Resource Guard `NORMAL` throughout; `oom_kill` unchanged (1323). |
| Key finding | Processes under the deploy user manager (Dagu **and the live executor**) carry groups `100 1001` only — no Docker socket. User units cannot add `SupplementaryGroups`. Docker/root delegation is therefore owner-only (system unit or root-owned helper), which is the intended boundary. Tests 3/4 passed only under a root-delegated transient unit granting the `docker` group to one invocation. |
| Security design | Dagu never receives the socket or sudo; a root-owned `mythos-hostops` helper (pattern of `mythos-logs`) enforces `ops/dagu-poc/hostops-allowlist.json` (READ none / WRITE+RESTART governance / DEPLOY owner / DESTRUCTIVE never); `tests/dagu-hostops-allowlist-test.js` 7/0. |
| Tests (as deploy, unmodified code) | resource-guard 91/0 · executor 390/0 · github-bridge 150/0 · action-resolution 88/0 · model-selection 81/0 · governance-invariant 111/0 · unattended-policy 53/0 · mcp-ecosystem 168/0 · allowlist 7/0. |
| Refused by the permission layer | `/etc/systemd/system/dagu-poc.service` (system unit). Fallback = deploy user unit. Owner items for production: `dagu` system user + unit, `60-dagu-hostops` sudoers line, the helper itself. |
| Decision | **RECOMMEND** as orchestration layer, conditional on the helper; next step = owner review, then a helper skeleton with the six READ verbs only. |

## gh-issue-118-r2 — Bridge reliability round 2: retry policy, runtime gate, lease expiry, inheritance rule (2026-09-03 10:59–11:25 UTC)

**Objective (GitHub Issue #118, rerun as `implement`, executor task `t-20260903105935-j13m7h`, OTHMODE OTH-2026-00052).**
Harden the GitHub Bridge with Temporal/Hatchet/Trigger.dev/Svix patterns as references only — no external runtime.
Attempt 1 (PR #119, `fc62b3f`, in base `41823e9`) already delivered the Action Resolution Engine, the action→profile
invariant before any provider, immutable attempt snapshots, fence/lease, runtime identity, synthesized reports and
the trail/resolve/runtime CLI. This attempt closes the gaps that remained against the 11 items.

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-118-r2` (worktree `…/worktrees/gh/gh-issue-118-r2`) over `41823e9` (latest `origin/main`). Delivery is the governance relay (`mythos-git-push.timer`, fast-forward only); **never pushed by the agent, never merged to main.** |
| §5 Retry policy | `lib/quota.js`: one classification per failure into `quota / transient / permission / governance / human / permanent` (`categorize`, `classifyOutcome`, precedence quota > governance > permission > human > transient > permanent), `RETRY_POLICY` as data, `classifyBlockedReport` (blocked report text → `GOVERNANCE_DENIED` / `PERMISSION_DENIED` / `HUMAN_APPROVAL`). Backoff `60 s × 4^n` capped 30 min with **additive jitter** `[base, 1.5×base]`, injectable random (`retryBaseDelayMs` / `retryDelayMs(n, random)`). `executor.js handleFailure`: governance and permission denials → `BLOCKED` (`GOVERNANCE_DENIED` / `PERMISSION_DENIED`), **zero automatic retries even with retries left**; transient → `WAITING_RETRY` with the schedule recorded. |
| §7/§10 Durable decision | `status.last_failure {category, code, retryable, timed_out, classified_at}`, `status.retry_backoff {attempt, max_retries, base_ms, delay_ms, jitter, max_ms}`, `status.transition_reason` on every failure/quota/preflight transition; `failure_classified` event; `lib/state.js transition()` puts `reason` on the `transition` event. Bridge report `execution` block carries `last_failure` / `retry_backoff` / `transition_reason`. |
| §9 Runtime gate | `github-bridge.js runtimeGate(runtime, cfg)` (pure, exported): `RUNTIME_IDENTITY_UNVERIFIED` and `RUNTIME_IDENTITY_MISMATCH` **refuse new claims by default** (previously only under `MYTHOS_BRIDGE_STRICT_RUNTIME=1`); opt-out `MYTHOS_BRIDGE_ALLOW_UNVERIFIED_RUNTIME=1` is recorded on the claim; `RUNTIME_STALE_CHECKOUT` still claims unless strict. Deferral carries `reason: runtime:<code>` + `detail` on the tick action, `claim_deferred` in the bridge log; `runtime` action shows `claims_allowed` / `gate_mode`; CLI `mythos-github-bridge runtime` prints the `gate`. Progress/report phases are never gated. |
| §4 Lease expiry | Progress phase: a non-terminal attempt past `execution.lease.expires_at` is recorded **once** (`lease.expired_noted_at`, `lease.expired_executor_status`, `LEASE_EXPIRED:` history note, `lease_expired` log + action) and never re-claimed or re-run — the executor owns its own recovery. |
| §3 Inheritance rule | `action-resolution.js resolveAction`: the `inherited_previous_attempt` candidate is `eligible:false` with `ignored_reason` when the previous `action_source` was `default` or absent (pre-engine record) — a rerun inherits a decision, never a default; `github-issues.js` keeps the refused candidate on `source.resolution.action_candidates` and in the notes. Behaviour change visible in the Issues suite: a rerun of a defaulted attempt is now `action_source: default`, not `inherited`. |
| Files | changed: `projects/mythos-ai-executor/lib/quota.js`, `executor.js`, `lib/state.js`, `bridge/action-resolution.js`, `bridge/github-bridge.js`, `bridge/github-issues.js`, `bin/mythos-github-bridge`; tests `tests/mythos-ai-executor-test.js`, `tests/mythos-github-bridge-test.js`, `tests/bridge-action-resolution-test.js`, `tests/mythos-github-issues-test.js`; docs `docs/MYTHOS_GITHUB_BRIDGE.md` (§12e), `docs/MYTHOS_GITHUB_ISSUES.md`, this file. **No protected path, no schema change** (`execution` / `source.resolution` are free objects), no `control/`, no service/policy/budget file. |
| Tests (as deploy, offline, mock provider, `MYTHOS_RESOURCE_GUARD=off` set by the suites) | `bridge-action-resolution` **88/0** (80 before) · `mythos-ai-executor` **390/0** (294) · `mythos-github-bridge` **150/0** (133) · `mythos-github-issues` **193/0** (192) · `model-selection-policy` **81/0** · `mythos-governance-invariant` **111/0** · `mythos-unattended-policy` **53/0** · `node --check` on every changed JS. The E2E `## Action: implement` → `requested_action=implement` → `repo-write` → provider (12d) still passes; `ACTION_PROFILE_MISMATCH` still stops before any provider (mock call count asserted). |
| Not done / owner | The running service keeps the old behaviour until the branch is merged and `mythos-ai-executor.service` / `mythos-github-bridge` restart (deployment is an owner action). The runtime gate default is stricter: a bridge whose module is not inside a git checkout now defers every claim until `MYTHOS_BRIDGE_ALLOW_UNVERIFIED_RUNTIME=1` is set deliberately — check `mythos-github-bridge runtime` after deploy. |
| Residual risk | Failure classification is regex-based on provider text; an unforeseen wording lands in `permanent` (`FAILED`, no retry) — visible, never silent, and `RETRY_POLICY`/patterns are one file. Lease expiry is observed, not enforced: a truly hung executor run still needs the executor's own timeout. |

## gh-issue-101 — MYTHOS Resource Guard (2026-09-02 23:35–23:58 UTC, re-delivered 2026-09-03 00:46–01:00 UTC)

**Objective (GitHub Issue #101, re-run as `implement`).** Actually protect the VPS: stop admitting new AI tasks
when host memory is unsafe, keep in-flight work resumable, and never let SWAP alone decide. Built on the
investigation in `control/reports/gh-issue-101.json` (Issue #101 attempt 1) — not re-investigated.

| Item | State |
|---|---|
| Branch / commit | Attempt r2 delivered `19e3d8a` + `dad5385` to `origin/mythos/gh/gh-issue-101-r2`, which was **never merged to main**. Attempt **r3** replays that identical tree onto the current main (`df8e285`, which carries the gh-issue-103 Issue-adapter merge): branch `mythos/gh/gh-issue-101-r3` (worktree `…/worktrees/gh/gh-issue-101-r3`), **`c5f3a35`** implementation + tests + spec, `56d7b41` the r2 handover text, `HEAD` this correction. The cherry-pick was conflict-free — gh-issue-103 touched `bridge/github-issues.js` and its own suite only, disjoint from every Resource Guard file; `git diff` of all Resource Guard paths against the r2 delivery is empty. |
| Commit-message correction | `c5f3a35`'s message is r2's, quoting r2's evidence (911-sample replay, github-issues 102/0). It was already on `origin` before this correction could be folded in, and the relay is fast-forward only, so it stands as written and **the r3 numbers in the rows below supersede it**. All r3 validation was re-run from scratch on the r3 base; nothing was reused as "previously verified". Delivery is the governance relay (`mythos-git-push.timer`, fast-forward only); **not merged to main**. |
| Before | Nothing read host memory. `drainQueue()` filtered `requested_by==='mos-console'`, so bridge tasks (`github-bridge`) were admitted by `tick()` step 4 with no gate at all; `drainQueue()` was edge-triggered only (no timer), so a queue held back with nothing running had no event left to restart it. |
| Signals | `MemAvailable` (`/proc/meminfo`) + PSI memory `some avg60` (`/proc/pressure/memory`) + `oom_kill` delta (`/proc/vmstat`). **Swap is reported, never a trigger** — this host sits at 96–100 % swap while healthy (live read during the run: 2416 MiB available, PSI 0.00, swap 96.2 % → `NORMAL`). |
| States | `NORMAL` / `WARNING` / `CRITICAL`, with `RECOVERED` as the one-shot alert kind on the degraded → `NORMAL` edge. CRITICAL enter `avail ≤ 700M` \|\| `psi60 ≥ 30` \|\| kill delta > 0; exit `≥ 1100M` && `≤ 10` && no kills. WARNING enter `≤ 1200M` \|\| `≥ 5`; exit `≥ 1600M` && `≤ 2`. 2 samples to escalate, 5 to de-escalate; a kill escalates on the first sample (a kill is a confirmed event, not a gauge). Alert cooldown 30 min per kind; a suppressed alert still records its transition. |
| Enforcement | **Admission only** — nothing is killed, no new status exists. Gated: `tick()` step 4 (the real admission point for n8n **and** `github-bridge` tasks), `dispatchTask()` (before the capacity check), `drainQueue()` (re-checked per iteration). Exempt: `tick()` steps 1–3 (interrupted recovery, quota resume, retry), so in-flight work stays resumable. A refused task stays `QUEUED` with one `dispatch_deferred` / `reason: resource_pressure` event per 10 min. |
| Recovery | `drainQueue()` is now called once per daemon step (the missing timer trigger), so the queue re-drains by itself after `RECOVERED` — proven end-to-end in the suite: a task deferred under `CRITICAL` runs with no manual re-queue once telemetry turns healthy. |
| Fail-open | Unreadable `/proc`, corrupt state, unwritable store → admit. 5 unreadable samples release a degraded level (`reason: telemetry_unavailable`). Kill switch `MYTHOS_RESOURCE_GUARD=off`. |
| Files | new: `projects/mythos-ai-executor/lib/resource-guard.js`, `projects/mythos-ai-executor/bin/mythos-resource-guard` (`status`/`sample`/`replay`), `tests/resource-guard-test.js`, `tests/fixtures/resource-guard/memwatch-{outage,healthy}.txt` (real telemetry, metric fields only; `.txt` because `.gitignore` excludes `*.log`), `docs/MYTHOS_RESOURCE_GUARD.md`. changed: `executor.js` (guard section, 3 gates, daemon re-drain, `resourceGuardStatus`), `server.js` (`GET /resource-guard`), `tests/mythos-ai-executor-test.js` + `tests/mythos-github-bridge-test.js` (set `MYTHOS_RESOURCE_GUARD=off` so their dispatch assertions do not depend on the host's live memory). **No protected path touched** (`executor.js` is not in `PROTECTED_PATHS`). |
| Historical replay | The **production** decision function replayed over `/opt/mythos-memwatch/memwatch.log`. r2: 911 samples (2026-09-01 17:21 → 2026-09-02 23:42). r3 re-ran it over the log as it now stands — **945 samples, 2026-09-01 17:21 → 2026-09-03 00:50**: 10 transitions, `NORMAL` 754 / `WARNING` 13 / `CRITICAL` 178 samples, `CRITICAL` continuously 2026-09-01 21:03 → 2026-09-02 02:16 (covering the 22:16–22:18 mass kill), `NORMAL` for the whole healthy remainder including the 34 samples added since r2, final level `NORMAL`. Swap ranged 54.3–100 % across that same window and moved nothing on its own. Admission was already closed **before every kill burst** in the outage excerpt — asserted, not narrated. |
| Tests (as deploy, offline, no quota, no real notification) | Re-run in full on the r3 base: `tests/resource-guard-test.js` **91/0** (new) · executor **265/0** · github-bridge **97/0** · github-issues **139/0** (the gh-issue-103 additions included; the ambient `MYTHOS_GITHUB_MCP_RW_TOKEN` was absent this run, so the gh-issue-100 environment failure did not arise) · model-selection **75/0** · mos-1-console **1438/0** · MOS-v2 regression gate **PASS (4 suites, 20/20 areas, 0 new failures)** · `node --check` on every changed JS file · live guard sample on the real host: `NORMAL`, 2424 MiB available, PSI 0.00, **swap 96 %** — the healthy-at-high-swap case, admitted. |
| Alerts | Through the existing `notify.sh` path (fire-and-forget) **plus** a durable `resource-guard-alerts.jsonl` ledger under `MYTHOS_EXECUTOR_HOME`. **WhatsApp stays unwired:** `bridge/notify/whatsapp.js` exists only on the sibling branch `mythos/gh/gh-20260902-wa-bridge-notify-01` (`b37491f`), whose tree also deletes `bridge/github-issues.js` — merging the two lines is an owner decision, and widening its `KINDS`/`KEY_RE` fence for WARNING/CRITICAL/RECOVERED is a second one. Because every alert is in the ledger, a sender added later loses no history. |
| Owner action still required | 1) the WhatsApp branch integration above; 2) essential-service protection (`MemoryLow=` on production units, `MemoryHigh=` on AI units) — systemd files under `projects/mythos-ai-executor/service/` (governance-protected) and `~deploy/.config/systemd/user/`, which no agent may install; 3) deploy: the running executor keeps its old behaviour until the branch is merged and `mythos-ai-executor.service` restarts. |
| Residual risk | The dominant consumer is outside MYTHOS control (root agent session scopes at 2–2.5 GB vs the executor's ~0.5–0.7 GB): the guard correctly refuses to start MYTHOS work in that state but cannot reclaim that memory, so MYTHOS work can be deferred for pressure MYTHOS did not cause — the replay shows one such ~5 h `CRITICAL` stretch on 2026-09-01. Only the systemd work above changes who loses memory first. |

## gh-issue-100 — Claude model selection policy (2026-09-02 23:00–23:20 UTC)

**Objective (GitHub Issue #100).** A TASK that names a model runs on that model; a TASK that names none gets an
automatic, explainable choice between Haiku / Sonnet / Opus; Fable stops being what runs by default and only
runs on explicit request. Path: GitHub Issue → bridge → executor, not rebuilt.

| Item | State |
|---|---|
| Branch / commit | `mythos/gh/gh-issue-100` (worktree `…/worktrees/gh/gh-issue-100`, base `dc45ff1`). Delivery is the governance relay; **not merged to main**. |
| Before | `executor.createTask` stored `model: input.model \|\| null`, no caller ever set it, and `providers/claude-code.js` only passed `--model` when it was set. So every autonomous run omitted `--model` and inherited the Claude CLI's own ambient default (the fable family). No `Model:` field existed anywhere in the Issue → control-task → executor path. |
| After | `lib/model-policy.js` + `config/model-policy.json` are the single decision point. `createTask` resolves a model for every `claude-code` task and records `model`, `model_selection_mode` (`explicit`/`auto`) and `model_selection_reason`; `buildArgs` now **always** emits `--model`. Explicit wins and is never substituted; absent is scored. |
| Auto rules | Additive integer signals: execution profile (`repo-read` 0 … `deploy` 3), task category (`investigate` 0 … `implement` 3), priority (−1/0/+1), instruction length (≥6000 +1, ≥12000 +2), complexity terms (+1 each, cap +3), simplicity terms (−1 each, cap −2), ≥6 constraints +1, ≥3 required tests +1. score ≤1 → **haiku**, ≥7 → **opus**, else **sonnet**. Every contributing signal is in the reason string, the task record, the `model_selected` event and the GitHub REPORT. Terms are plain substrings chosen not to occur in the bridge's own instruction boilerplate — a test pins that, so GitHub tasks are not pushed up a tier by their own template. |
| Fable | Every fable catalog entry is `auto_selectable: false`, and the three auto tiers are validated to name auto-selectable entries only. A test sweeps 648 signal combinations (including an instruction that asks for fable in prose) and none reaches it. |
| `Model: Fable 5.1` | **Refused, by decision.** The installed Claude CLI (2.1.251) has no such model — its catalog offers `claude-fable-5` only (`claude-fable-5-1` does not exist in the binary). Substituting Fable 5 would violate "never replace a named model", so the catalog carries `fable-5.1` as `enabled: false` with the reason, and the request is rejected at intake with that message. **Owner decision needed:** flip `enabled` to `true` in `config/model-policy.json` (no code change) once a CLI that accepts `claude-fable-5-1` is installed. |
| Security | A task/Issue can only *select a catalog key*; it can never supply a model string. Every catalog `model` is re-validated against `/^[a-z0-9][a-z0-9._-]{1,63}$/`, so no value reaching argv can carry whitespace or look like another CLI flag. `fallback_model` passes the same allow-list. Model choice grants no authority: provider, execution profile, working directory, tools, MCP capabilities and credentials remain server-side. No Resource Guard change was needed. |
| Fail-safe | An unreadable or invalid `config/model-policy.json` falls back to a built-in policy (logged on stderr) instead of disabling the layer — "no policy" would mean "no `--model`", i.e. the fable default returning. |
| Files | new: `projects/mythos-ai-executor/lib/model-policy.js`, `projects/mythos-ai-executor/config/model-policy.json`, `tests/model-selection-policy-test.js`. changed: `executor.js`, `providers/claude-code.js`, `schemas/task.schema.json` (+`model_selection_mode`, `model_selection_reason`), `bridge/github-bridge.js` (validate + pass through + report + fingerprint), `bridge/github-issues.js` (`Model:` / `النموذج:` section, `model:<x>` label, intake rejection, created-comment row), `bridge/schemas/task.schema.json` (+`model`), `bridge/README.md`, `docs/MYTHOS_GITHUB_BRIDGE.md`, `docs/MYTHOS_GITHUB_ISSUES.md`. No protected path touched. |
| Tests (all as deploy, offline, no quota spent) | `tests/model-selection-policy-test.js` **75/0** (new) · executor **265/0** · github-bridge **97/0** · github-issues **102/0** · core-wiring **86/0** · orchestration-core **257/0** · orchestrator-0 **156/0** · n8n-bridge **80/0** · othk-2w **42/0** · othmode-3 **94/0** · `node --check` on all changed JS + JSON parse on all changed schemas. |
| Test-environment notes | The github-issues suite reports 2 failures when run inside the executor service environment because `MYTHOS_GITHUB_MCP_RW_TOKEN` is exported there and two assertions expect *no* token to be resolvable; with that variable removed the suite is 102/0. Pre-existing and unrelated to this change. `tests/core-test.js` still fails at load with the known `_memCache is not defined` (browser-app core, untouched here). `tests/mos-e2e-lifecycle-test.js` refuses to run on a host that has the real mythos-prod checkout, by its own design. |
| Restart / recovery | No new service or state store. The decision is persisted in the task record, so a resumed run (`--resume`) replays the same `--model` as the first run — asserted in the new suite. |

## MYTHOS-GITHUB-ISSUES-0 — GitHub Issues → TASK channel (2026-09-02 19:45–20:20 UTC)

**Objective.** Make GitHub Issues the official task intake without rebuilding MCP/ContextForge/OTHMODE/executor/bridge: Issue → `control/tasks/<id>.json` → existing execution → `control/reports/<id>.json` → Issue updated. Spec + Issue format: `docs/MYTHOS_GITHUB_ISSUES.md`.

| Item | State |
|---|---|
| Branch / commits | `mythos/github-issues-20260902` (worktree `/home/deploy/worktrees/github-issues`, base `origin/main` ccedcbc): `faf720c` implementation + tests + docs, `4013781` schema formatting, then the docs commit carrying this entry. Delivered by the relay (`pushed=1`). **Not merged to main** — owner decision. |
| Reused, unchanged | bridge state machine, claim/report/index code paths, executor, OTHMODE, redact/schema libs, relay. Bridge diff: +22 lines (exports, `source` in index rows, one instruction sentence). No protected path touched. |
| Added | `bridge/github-issues.js` (adapter), `bridge/systemd/mythos-github-bridge.service.d/issues.conf.example`, CLI `issues-tick|issues-status|issues-parse`, `tests/mythos-github-issues-test.js` (102/0), `tests/fixtures/github-issues/issue-95.json`, `docs/MYTHOS_GITHUB_ISSUES.md`; `source` block in `bridge/schemas/task.schema.json`; README/spec pointers. |
| Tests | issues 102/0 · bridge 97/0 · executor 265/0 · governance-invariant 111/0 (all as deploy, offline). Covered: new Issue → task, duplicate prevention (repeat, cache loss, worktree rebuilt from origin, crash between comment and commit), two concurrent intakes, CLAIMED + executor_task_id comment, COMPLETED/FAILED/BLOCKED/HUMAN_APPROVAL/CANCELLED report comments, delivery follow-up, depends_on wait, rerun, close policy, secret rejection (no secret in requests/comments/tree/history/log), dry-run, `--only`, no label / closed / PR never converted, wrong user, missing token, shared lock, main byte-for-byte untouched, control-only commits. |
| Live verification | read-only on the real repo as deploy from the worktree with the existing PAT: `issues-parse 95` → valid `gh-issue-95` (objective from الهدف, 12 scope items from المطلوب, 6 validation items, `investigate` by default because no Action is stated); `issues-tick --dry-run --only 95` → `would_create gh-issue-95`, 1 API call, nothing written (#95: 0 comments, control branch unchanged). #95 was NOT executed (it would edit Status Center). |
| Live write path | **BLOCKED (owner credential).** Smoke Issue #96 (`TASK: github-issues adapter smoke — read-only (safe)`) was created with the PAT, but the same PAT gets HTTP 403 on `POST /issues/96/labels` and `POST /issues/96/comments` → `cred_github_gateway` lacks `Issues: write` (creating the Issue only worked through permissionless public-repo access). #96 therefore has no `task` label and no adapter comment yet. |
| Owner steps | 1) fine-grained PAT `Issues: Read and write` on othoth77/mythos-prod → `/home/deploy/deployments/mythos-gateway/github-issues.env` (`MYTHOS_GITHUB_ISSUES_TOKEN=…`, 0600, `read -s`, `umask 077`), inventory entry `cred_github_issues`; 2) add label `task` to #96 (UI) and run as deploy from the worktree `node projects/mythos-ai-executor/bin/mythos-github-bridge issues-tick --only 96` twice (≈2 min apart, after the executor finishes) → created/claimed/report comments on #96; 3) merge the branch into main (`git merge --no-ff origin/mythos/github-issues-20260902` on the shared checkout, operator shell); 4) install the drop-in (`issues.conf.example` → `~deploy/.config/systemd/user/mythos-github-bridge.service.d/issues.conf`, `systemctl --user daemon-reload`) — ONLY after main carries the schema change; 5) decide #95: add `Action: implement` (or label `action:implement`) if the write run is intended, otherwise it runs read-only at the first enabled tick. |
| Not touched | AUTOS, SPY, n8n, MCP/ContextForge, OAuth, WhatsApp, production configuration, main, protected paths. |
| Housekeeping | #96 stays open as the smoke fixture until the write path is verified; the shared checkout `main` (d9479a1) is 2 behind `origin/main` (ccedcbc) — pre-existing, the relay reports it every tick. |

## 2026-09-02 — GitHub bridge WhatsApp notification layer (`gh-20260902-wa-bridge-notify-01`)

- Scope: added an optional, disabled-by-default WhatsApp notification layer to the existing MYTHOS GitHub bridge. Bridge notifications only — explicitly **not** MYTHOS AUTO WhatsApp / customer chat.
- Branch: `mythos/gh/gh-20260902-wa-bridge-notify-01`, based on `ccedcbc50850`. Dispatched from `control/tasks/gh-20260902-wa-bridge-notify-01.json`; OTHMODE record `OTH-2026-00024` (closed by the bridge, not by this session).
- Added: `projects/mythos-ai-executor/bridge/notify/whatsapp.js` (notification layer, idempotency ledger, retry), `bridge/notify/http-json.js` (Node-core JSON POST, no npm dependency), `bridge/notify/providers/evolution.js` (Evolution API adapter, `sendText` only), `tests/mythos-bridge-whatsapp-notify-test.js`, `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md`.
- Changed: `bridge/github-bridge.js` (one `require`, one enqueue call in `finishTask()`, `human_approval: true` on the blocked-for-a-human path, `flushNotifications()`, daemon flush), `bin/mythos-github-bridge` (`notify-config|notify-status|notify-flush|notify-test --confirm`), `docs/MYTHOS_GITHUB_BRIDGE.md` §12c, `projects/command-center/data/open-source-registry.json` (Search First verdict, append-only).
- Design: two-phase. `finishTask()` only writes a durable ledger entry (local, synchronous, **never network**); delivery happens in `flushNotifications()` **after** `tick()` has returned. A gateway outage therefore cannot slow, fail or alter a tick — verified by asserting the TASK and REPORT files are byte-identical and no control commit is produced while the gateway returns 500.
- Notifies on `COMPLETED`, `FAILED`, `BLOCKED` and `HUMAN_APPROVAL` only. `HUMAN_APPROVAL` is a notification kind for the bridge's existing blocked-for-a-human condition (claim exists, executor record gone); `mythos-control/1` and the control status set are **unchanged**.
- Idempotency: durable ledger keyed `<task_id>__<KIND>` under `$MYTHOS_BRIDGE_HOME/notify/ledger/` (existing executor store convention, no database added), with an `O_EXCL` lock per key. Duplicate polling, four parallel in-process flushes, four concurrent OS processes, and restart-after-crash all yield exactly one message per recipient.
- Tests run in this session: `mythos-bridge-whatsapp-notify-test.js` **116 passed / 0 failed** (new); `mythos-github-bridge-test.js` 97/0; `mythos-ai-executor-test.js` 265/0; `mythos-governance-invariant-test.js` 111/0; `othmode-3-tasks-test.js` 94/0; `othmode-2-platform-test.js` 141/0 (http section skipped: no `pg` on this host). `mcc-1-command-center-test.js` could not run — pre-existing `MODULE_NOT_FOUND: pg` in the untouched `projects/command-center/reference/db.js`, the same host limitation othmode-2 skips for.
- No real WhatsApp message is sent by any test: the far end is a local `http.createServer` on `127.0.0.1`, while the real adapter and real HTTP path are exercised.
- **Not done, deliberately:** the provider is **not deployed** and the one controlled real smoke test is **not performed**. No WhatsApp gateway exists on this host, the `deploy` user has no Docker access (`/var/run/docker.sock` permission denied), and swap is fully consumed (`free -m`: 4095/4095 MiB used, ~425 MiB RAM free) — provisioning a provider is a separate privileged deployment task and unsafe to attempt under that pressure.
- **Provider choice is PROVISIONAL.** This run had no outbound network access (`WebFetch`/`WebSearch` unavailable, direct HTTPS blocked), so upstream facts about Evolution API, WAHA, MultiWA and WaSphere could not be live-verified and are recorded `TO-VERIFY`. The mitigation is architectural: the provider sits behind an adapter, so switching provider — or migrating to the official WhatsApp Business Cloud API — is a new file plus one line in the `PROVIDERS` map, with `github-bridge.js` unchanged.
- Governance: no `git push` run (delivery is the `mythos-git-push` relay), nothing under `control/` edited, no governance-protected path touched, no unit file in Git carries a credential (configuration is a systemd drop-in outside the repository).
- Next step: the deployment task — address the swap pressure, live-verify the provider (licence, maintenance, real footprint) and re-open the choice if the footprint does not fit, deploy the gateway on a private network, configure the drop-in per `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md` §4.1, then run `mythos-github-bridge notify-test --confirm` once.

**Last updated:** 2026-09-02 20:20 UTC
**From:** MYTHOS-GITHUB-ISSUES-0 — **GitHub Issues are now the official TASK intake.** `projects/mythos-ai-executor/bridge/github-issues.js` converts open Issues labelled `task` into `control/tasks/gh-issue-<n>.json` (PENDING, `source` block = the only schema addition), lets the UNCHANGED bridge/executor/OTHMODE run them, and posts created / claimed / report comments (status, summary, files, tests, commit SHAs, problems, next action) + status labels from the control files; HUMAN_APPROVAL derived from owner-decision BLOCKED reports; COMPLETED never closes an Issue unless `MYTHOS_ISSUES_CLOSE_ON_COMPLETED=1`; closing an Issue cancels its task; `rerun` label → new attempt. Idempotent (file-after-sync, shared lock, comment markers, hash-keyed rejections). Suites: issues 102/0 (fake GitHub API, real #95 payload parse-only), bridge 97/0, executor 265/0, governance-invariant 111/0. Live read path verified on #95 (dry-run: would create `gh-issue-95` as `investigate`; nothing written). **Live write path BLOCKED on a credential:** the only PAT (`cred_github_gateway`) gets 403 on comments/labels → owner issues a PAT with `Issues: Read and write` (see `docs/MYTHOS_GITHUB_ISSUES.md` §6) and installs the drop-in. Branch `mythos/github-issues-20260902` (`faf720c`, `4013781`, + this entry) on origin; NOT merged.**

## 2026-09-02 — Status Center discovery refresh

- Scope: refreshed `projects/status-center/data/repo-snapshot.json` from the current GitHub `othoth77` repository inventory.
- Verified new repository discovery: `othoth77/spy`, latest commit `41b3e04f53b401ca60380ba11a11a1c9fff1e871`, dated 2026-09-01T17:41:37Z, is now included in the snapshot and will surface as `NEW_DISCOVERY` until explicitly classified in the Status Center registry.
- Commit: `bceb38743ec442e7f28a46e5c7ceb997b9ba6185`.
- Remote `main` HEAD verified after write: `bceb38743ec442e7f28a46e5c7ceb997b9ba6185`.
- Verification: GitHub contents and commit metadata confirm the snapshot update is present on `main`.
- Limitation: the review engine could not be executed from this environment and the live VPS/status host could not be reached. Therefore no new immutable `REVIEW-*` snapshot, `data/current.json` publication, live `/health` verification, or production deployment is claimed by this handover entry.
- Next step: execute `node projects/status-center/bin/review.js`, verify the generated immutable review/current data, run `node tests/stc-1-status-center-test.js`, and publish the resulting Status Center snapshot through the sanctioned production deployment path.
