# AI Handover

## gh-issue-118-r2 — Bridge reliability round 2: retry policy, runtime gate, lease expiry, inheritance rule (2026-09-03 10:59–11:25 UTC)

**Objective (GitHub Issue #118, rerun as `implement`, executor task `t-20260903105935-j13m7h`, OTHMODE OTH-2026-00052).**
Harden the GitHub Bridge with Temporal/Hatchet/Trigger.dev/Svix patterns as references only — no external runtime.
Attempt 1 (PR #119, `fc62b3f`, in base `41823e9`) already delivered the Action Resolution Engine, the action→profile
invariant before any provider, immutable attempt snapshots, fence/lease, runtime identity, synthesized reports and
the trail/resolve/runtime CLI. This attempt closes the gaps that remained against the 11 items.

| Item | State |
|---|---|
| Branch | `mythos/gh/gh-issue-118-r2` (worktree `…/worktrees/gh/gh-issue-118-r2`) over `41823e9` (latest `origin/main`). Delivery is the governance relay (`mythos-git-push.timer`, fast-forward only); **never pushed by the agent, never merged to main.** |
| §5 Retry policy | `lib/quota.js`: one classification per failure into `quota / transient / permission / governance / human / permanent` (`categorize`, `classifyOutcome`, precedence quota > governance > permission > human > transient > permanent), `RETRY_POLICY` as data, `classifyBlockedReport` (blocked report text → `GOVERNANCE_DENIED` / `PERMISSION_DENIED` / `HUMAN_APPROVAL`). Backoff `60 s × 4^n` capped 30 min with **additive jitter** `[base, 1.5×base]`, injectable random (`retryBaseDelayMs` / `retryDelayMs(n, random)`). `executor.js handleFailure`: governance and permission denials → `BLOCKED` (`GOVERNANCE_DENIED` / `PERMISSION_DENIED`), **zero automatic retries even with retries left**; transient → `WAITING_RETRY` with the schedule recorded. |
| §7/§10 Durable decision | `status.last_failure {category, code, retryable, timed_out, classified_at}`, `status.retry_backoff {attempt, max_retries, base_ms, delay_ms, jitter, max_ms}`, `status.transition_reason` on every failure/quota/preflight transition; `failure_classified` event; `lib/state.js transition()` puts `reason` on the `transition` event. Bridge report `execution` block carries `last_failure` / `retry_backoff` / `transition_reason`. |
| §9 Runtime gate | `github-bridge.js runtimeGate(runtime, cfg)` (pure, exported): `RUNTIME_IDENTITY_UNVERIFIED` and `RUNTIME_IDENTITY_MISMATCH` **refuse new claims by default** (previously only under `MYTHOS_BRIDGE_STRICT_RUNTIME=1`); opt-out `MYTHOS_BRIDGE_ALLOW_UNVERIFIED_RUNTIME=1` is recorded on the claim; `RUNTIME_STALE_CHECKOUT` still claims unless strict. Deferral carries `reason: runtime:<code>` + `detail` on the tick action, `claim_deferred` in the bridge log; `runtime` action shows `claims_allowed` / `gate_mode`; CLI `mythos-github-bridge runtime` prints the `gate`. Progress/report phases are never gated. |
| §4 Lease expiry | Progress phase: a non-terminal attempt past `execution.lease.expires_at` is recorded **once** (`lease.expired_noted_at`, `lease.expired_executor_status`, `LEASE_EXPIRED:` history note, `lease_expired` log + action) and never re-claimed or re-run — the executor owns its own recovery. |
| §3 Inheritance rule | `action-resolution.js resolveAction`: the `inherited_previous_attempt` candidate is `eligible:false` with `ignored_reason` when the previous `action_source` was `default` or absent (pre-engine record) — a rerun inherits a decision, never a default; `github-issues.js` keeps the refused candidate on `source.resolution.action_candidates` and in the notes. Behaviour change visible in the Issues suite: a rerun of a defaulted attempt is now `action_source: default`, not `inherited`. |
| Files | changed: `projects/mythos-ai-executor/lib/quota.js`, `executor.js`, `lib/state.js`, `bridge/action-resolution.js`, `bridge/github-bridge.js`, `bridge/github-issues.js`, `bin/mythos-github-bridge`; tests `tests/mythos-ai-executor-test.js`, `tests/mythos-github-bridge-test.js`, `tests/bridge-action-resolution-test.js`, `tests/mythos-github-issues-test.js`; docs `docs/MYTHOS_GITHUB_BRIDGE.md` (§12e), `docs/MYTHOS_GITHUB_ISSUES.md`, this file. **No protected path, no schema change** (`execution` / `source.resolution` are free objects), no `control/`, no service/policy/budget file. |
| Tests (as deploy, offline, mock provider, `MYTHOS_RESOURCE_GUARD=off` set by the suites) | `bridge-action-resolution` **88/0** (80 before) · `mythos-ai-executor` **390/0** (294) · `mythos-github-bridge` **150/0** (133) · `mythos-github-issues` **193/0** (192) · `model-selection-policy` **81/0** · `mythos-governance-invariant` **111/0** · `mythos-unattended-policy` **53/0** · `node --check` on every changed JS. The E2E `## Action: implement` → `requested_action=implement` → `repo-write` → provider (12d) still passes; `ACTION_PROFILE_MISMATCH` still stops before any provider (mock call count asserted). |
| Not done / owner | The running service keeps the old behaviour until the branch is merged and `mythos-ai-executor.service` / `mythos-github-bridge` restart (deployment is an owner action). The runtime gate default is stricter: a bridge whose module is not inside a git checkout now defers every claim until `MYTHOS_BRIDGE_ALLOW_UNVERIFIED_RUNTIME=1` is set deliberately — check `mythos-github-bridge runtime` after deploy. |
| Residual risk | Failure classification is regex-based on provider text; an unforeseen wording lands in `permanent` (`FAILED`, no retry) — visible, never silent, and `RETRY_POLICY`/patterns are one file. Lease expiry is observed, not enforced: a truly hung executor run still needs the executor's own timeout. |

## gh-issue-101 — MYTHOS Resource Guard (2026-09-02 23:35–23:58 UTC, re-delivered 2026-09-03 00:46–01:00 UTC)

**Objective (GitHub Issue #101, re-run as `implement`).** Actually protect the VPS: stop admitting new AI tasks
when host memory is unsafe, keep in-flight work resumable, and never let SWAP alone decide. Built on the
investigation in `control/reports/gh-issue-101.json` (Issue #101 attempt 1) — not re-investigated.

| Item | State |
|---|---|
| Branch / commit | Attempt r2 delivered `19e3d8a` + `dad5385` to `origin/mythos/gh/gh-issue-101-r2`, which was **never merged to main**. Attempt **r3** replays that identical tree onto the current main (`df8e285`, which carries the gh-issue-103 Issue-adapter merge): branch `mythos/gh/gh-issue-101-r3` (worktree `…/worktrees/gh/gh-issue-101-r3`), **`c5f3a35`** implementation + tests + spec, `56d7b41` the r2 handover text, `HEAD` this correction. The cherry-pick was conflict-free — gh-issue-103 touched `bridge/github-issues.js` and its own suite only, disjoint from every Resource Guard file; `git diff` of all Resource Guard paths against the r2 delivery is empty. |
| Commit-message correction | `c5f3a35`'s message is r2's, quoting r2's evidence (911-sample replay, github-issues 102/0). It was already on `origin` before this correction could be folded in, and the relay is fast-forward only, so it stands as written and **the r3 numbers in the rows below supersede it**. All r3 validation was re-run from scratch on the r3 base; nothing was reused as "previously verified". Delivery is the governance relay (`mythos-git-push.timer`, fast-forward only); **not merged to main**. |
| Before | Nothing read host memory. `drainQueue()` filtered `requested_by==='mos-console'`, so bridge tasks (`github-bridge`) were admitted by `tick()` step 4 with no gate at all; `drainQueue()` was edge-triggered only (no timer), so a queue held back with nothing running had no event left to restart it. |
| Signals | `MemAvailable` (`/proc/meminfo`) + PSI memory `some avg60` (`/proc/pressure/memory`) + `oom_kill` delta (`/proc/vmstat`). **Swap is reported, never a trigger** — this host sits at 96–100 % swap while healthy (live read during the run: 2416 MiB available, PSI 0.00, swap 96.2 % → `NORMAL`). |
| States | `NORMAL` / `WARNING` / `CRITICAL`, with `RECOVERED` as the one-shot alert kind on the degraded → `NORMAL` edge. CRITICAL enter `avail ≤ 700M` \|\| `psi60 ≥ 30` \|\| kill delta > 0; exit `≥ 1100M` && `≤ 10` && no kills. WARNING enter `≤ 1200M` \|\| `≥ 5`; exit `≥ 1600M` && `≤ 2`. 2 samples to escalate, 5 to de-escalate; a kill escalates on the first sample (a kill is a confirmed event, not a gauge). Alert cooldown 30 min per kind; a suppressed alert still records its transition. |
| Enforcement | **Admission only** — nothing is killed, no new status exists. Gated: `tick()` step 4 (the real admission point for n8n **and** `github-bridge` tasks), `dispatchTask()` (before the capacity check), `drainQueue()` (re-checked per iteration). Exempt: `tick()` steps 1–3 (interrupted recovery, quota resume, retry), so in-flight work stays resumable. A refused task stays `QUEUED` with one `dispatch_deferred` / `reason: resource_pressure` event per 10 min. |
| Recovery | `drainQueue()` is now called once per daemon step (the missing timer trigger), so the queue re-drains by itself after `RECOVERED` — proven end-to-end in the suite: a task deferred under `CRITICAL` runs with no manual re-queue once telemetry turns healthy. |
| Fail-open | Unreadable `/proc`, corrupt state, unwritable store → admit. 5 unreadable samples release a degraded level (`reason: telemetry_unavailable`). Kill switch `MYTHOS_RESOURCE_GUARD=off`. |
| Files | new: `projects/mythos-ai-executor/lib/resource-guard.js`, `projects/mythos-ai-executor/bin/mythos-resource-guard` (`status`/`sample`/`replay`), `tests/resource-guard-test.js`, `tests/fixtures/resource-guard/memwatch-{outage,healthy}.txt` (real telemetry, metric fields only; `.txt` because `.gitignore` excludes `*.log`), `docs/MYTHOS_RESOURCE_GUARD.md`. changed: `executor.js` (guard section, 3 gates, daemon re-drain, `resourceGuardStatus`), `server.js` (`GET /resource-guard`), `tests/mythos-ai-executor-test.js` + `tests/mythos-github-bridge-test.js` (set `MYTHOS_RESOURCE_GUARD=off` so their dispatch assertions do not depend on the host's live memory). **No protected path touched** (`executor.js` is not in `PROTECTED_PATHS`). |
| Historical replay | The **production** decision function replayed over `/opt/mythos-memwatch/memwatch.log`. r2: 911 samples (2026-09-01 17:21 → 2026-09-02 23:42). r3 re-ran it over the log as it now stands — **945 samples, 2026-09-01 17:21 → 2026-09-03 00:50**: 10 transitions, `NORMAL` 754 / `WARNING` 13 / `CRITICAL` 178 samples, `CRITICAL` continuously 2026-09-01 21:03 → 2026-09-02 02:16 (covering the 22:16–22:18 mass kill), `NORMAL` for the whole healthy remainder including the 34 samples added since r2, final level `NORMAL`. Swap ranged 54.3–100 % across that same window and moved nothing on its own. Admission was already closed **before every kill burst** in the outage excerpt — asserted, not narrated. |
| Tests (as deploy, offline, no quota, no real notification) | Re-run in full on the r3 base: `tests/resource-guard-test.js` **91/0** (new) · executor **265/0** · github-bridge **97/0** · github-issues **139/0** (the gh-issue-103 additions included; the ambient `MYTHOS_GITHUB_MCP_RW_TOKEN` was absent this run, so the gh-issue-100 environment failure did not arise) · model-selection **75/0** · mos-1-console **1438/0** · MOS-v2 regression gate **PASS (4 suites, 20/20 areas, 0 new failures)** · `node --check` on every changed JS file · live guard sample on the real host: `NORMAL`, 2424 MiB available, PSI 0.00, **swap 96 %** — the healthy-at-high-swap case, admitted. |
| Alerts | Through the existing `notify.sh` path (fire-and-forget) **plus** a durable `resource-guard-alerts.jsonl` ledger under `MYTHOS_EXECUTOR_HOME`. **WhatsApp stays unwired:** `bridge/notify/whatsapp.js` exists only on the sibling branch `mythos/gh/gh-20260902-wa-bridge-notify-01` (`b37491f`), whose tree also deletes `bridge/github-issues.js` — merging the two lines is an owner decision, and widening its `KINDS`/`KEY_RE` fence for WARNING/CRITICAL/RECOVERED is a second one. Because every alert is in the ledger, a sender added later loses no history. |
| Owner action still required | 1) the WhatsApp branch integration above; 2) essential-service protection (`MemoryLow=` on production units, `MemoryHigh=` on AI units) — systemd files under `projects/mythos-ai-executor/service/` (governance-protected) and `~deploy/.config/systemd/user/`, which no agent may install; 3) deploy: the running executor keeps its old behaviour until the branch is merged and `mythos-ai-executor.service` restarts. |
| Residual risk | The dominant consumer is outside MYTHOS control (root agent session scopes at 2–2.5 GB vs the executor's ~0.5–0.7 GB): the guard correctly refuses to start MYTHOS work in that state but cannot reclaim that memory, so MYTHOS work can be deferred for pressure MYTHOS did not cause — the replay shows one such ~5 h `CRITICAL` stretch on 2026-09-01. Only the systemd work above changes who loses memory first. |

## gh-issue-100 — Claude model selection policy (2026-09-02 23:00–23:20 UTC)

**Objective (GitHub Issue #100).** A TASK that names a model runs on that model; a TASK that names none gets an
automatic, explainable choice between Haiku / Sonnet / Opus; Fable stops being what runs by default and only
runs on explicit request. Path: GitHub Issue → bridge → executor, not rebuilt.

| Item | State |
|---|---|
| Branch / commit | `mythos/gh/gh-issue-100` (worktree `…/worktrees/gh/gh-issue-100`, base `dc45ff1`). Delivery is the governance relay; **not merged to main**. |
| Before | `executor.createTask` stored `model: input.model \|\| null`, no caller ever set it, and `providers/claude-code.js` only passed `--model` when it was set. So every autonomous run omitted `--model` and inherited the Claude CLI's own ambient default (the fable family). No `Model:` field existed anywhere in the Issue → control-task → executor path. |
| After | `lib/model-policy.js` + `config/model-policy.json` are the single decision point. `createTask` resolves a model for every `claude-code` task and records `model`, `model_selection_mode` (`explicit`/`auto`) and `model_selection_reason`; `buildArgs` now **always** emits `--model`. Explicit wins and is never substituted; absent is scored. |
| Auto rules | Additive integer signals: execution profile (`repo-read` 0 … `deploy` 3), task category (`investigate` 0 … `implement` 3), priority (−1/0/+1), instruction length (≥6000 +1, ≥12000 +2), complexity terms (+1 each, cap +3), simplicity terms (−1 each, cap −2), ≥6 constraints +1, ≥3 required tests +1. score ≤1 → **haiku**, ≥7 → **opus**, else **sonnet**. Every contributing signal is in the reason string, the task record, the `model_selected` event and the GitHub REPORT. Terms are plain substrings chosen not to occur in the bridge's own instruction boilerplate — a test pins that, so GitHub tasks are not pushed up a tier by their own template. |
| Fable | Every fable catalog entry is `auto_selectable: false`, and the three auto tiers are validated to name auto-selectable entries only. A test sweeps 648 signal combinations (including an instruction that asks for fable in prose) and none reaches it. |
| `Model: Fable 5.1` | **Refused, by decision.** The installed Claude CLI (2.1.251) has no such model — its catalog offers `claude-fable-5` only (`claude-fable-5-1` does not exist in the binary). Substituting Fable 5 would violate "never replace a named model", so the catalog carries `fable-5.1` as `enabled: false` with the reason, and the request is rejected at intake with that message. **Owner decision needed:** flip `enabled` to `true` in `config/model-policy.json` (no code change) once a CLI that accepts `claude-fable-5-1` is installed. |
| Security | A task/Issue can only *select a catalog key*; it can never supply a model string. Every catalog `model` is re-validated against `/^[a-z0-9][a-z0-9._-]{1,63}$/`, so no value reaching argv can carry whitespace or look like another CLI flag. `fallback_model` passes the same allow-list. Model choice grants no authority: provider, execution profile, working directory, tools, MCP capabilities and credentials remain server-side. No Resource Guard change was needed. |
| Fail-safe | An unreadable or invalid `config/model-policy.json` falls back to a built-in policy (logged on stderr) instead of disabling the layer — "no policy" would mean "no `--model`", i.e. the fable default returning. |
| Files | new: `projects/mythos-ai-executor/lib/model-policy.js`, `projects/mythos-ai-executor/config/model-policy.json`, `tests/model-selection-policy-test.js`. changed: `executor.js`, `providers/claude-code.js`, `schemas/task.schema.json` (+`model_selection_mode`, `model_selection_reason`), `bridge/github-bridge.js` (validate + pass through + report + fingerprint), `bridge/github-issues.js` (`Model:` / `النموذج:` section, `model:<x>` label, intake rejection, created-comment row), `bridge/schemas/task.schema.json` (+`model`), `bridge/README.md`, `docs/MYTHOS_GITHUB_BRIDGE.md`, `docs/MYTHOS_GITHUB_ISSUES.md`. No protected path touched. |
| Tests (all as deploy, offline, no quota spent) | `tests/model-selection-policy-test.js` **75/0** (new) · executor **265/0** · github-bridge **97/0** · github-issues **102/0** · core-wiring **86/0** · orchestration-core **257/0** · orchestrator-0 **156/0** · n8n-bridge **80/0** · othk-2w **42/0** · othmode-3 **94/0** · `node --check` on all changed JS + JSON parse on all changed schemas. |
| Test-environment notes | The github-issues suite reports 2 failures when run inside the executor service environment because `MYTHOS_GITHUB_MCP_RW_TOKEN` is exported there and two assertions expect *no* token to be resolvable; with that variable removed the suite is 102/0. Pre-existing and unrelated to this change. `tests/core-test.js` still fails at load with the known `_memCache is not defined` (browser-app core, untouched here). `tests/mos-e2e-lifecycle-test.js` refuses to run on a host that has the real mythos-prod checkout, by its own design. |
| Restart / recovery | No new service or state store. The decision is persisted in the task record, so a resumed run (`--resume`) replays the same `--model` as the first run — asserted in the new suite. |

## MYTHOS-GITHUB-ISSUES-0 — GitHub Issues → TASK channel (2026-09-02 19:45–20:20 UTC)

**Objective.** Make GitHub Issues the official task intake without rebuilding MCP/ContextForge/OTHMODE/executor/bridge: Issue → `control/tasks/<id>.json` → existing execution → `control/reports/<id>.json` → Issue updated. Spec + Issue format: `docs/MYTHOS_GITHUB_ISSUES.md`.

| Item | State |
|---|---|
| Branch / commits | `mythos/github-issues-20260902` (worktree `/home/deploy/worktrees/github-issues`, base `origin/main` ccedcbc): `faf720c` implementation + tests + docs, `4013781` schema formatting, then the docs commit carrying this entry. Delivered by the relay (`pushed=1`). **Not merged to main** — owner decision. |
| Reused, unchanged | bridge state machine, claim/report/index code paths, executor, OTHMODE, redact/schema libs, relay. Bridge diff: +22 lines (exports, `source` in index rows, one instruction sentence). No protected path touched. |
| Added | `bridge/github-issues.js` (adapter), `bridge/systemd/mythos-github-bridge.service.d/issues.conf.example`, CLI `issues-tick|issues-status|issues-parse`, `tests/mythos-github-issues-test.js` (102/0), `tests/fixtures/github-issues/issue-95.json`, `docs/MYTHOS_GITHUB_ISSUES.md`; `source` block in `bridge/schemas/task.schema.json`; README/spec pointers. |
| Tests | issues 102/0 · bridge 97/0 · executor 265/0 · governance-invariant 111/0 (all as deploy, offline). Covered: new Issue → task, duplicate prevention (repeat, cache loss, worktree rebuilt from origin, crash between comment and commit), two concurrent intakes, CLAIMED + executor_task_id comment, COMPLETED/FAILED/BLOCKED/HUMAN_APPROVAL/CANCELLED report comments, delivery follow-up, depends_on wait, rerun, close policy, secret rejection (no secret in requests/comments/tree/history/log), dry-run, `--only`, no label / closed / PR never converted, wrong user, missing token, shared lock, main byte-for-byte untouched, control-only commits. |
| Live verification | read-only on the real repo as deploy from the worktree with the existing PAT: `issues-parse 95` → valid `gh-issue-95` (objective from الهدف, 12 scope items from المطلوب, 6 validation items, `investigate` by default because no Action is stated); `issues-tick --dry-run --only 95` → `would_create gh-issue-95`, 1 API call, nothing written (#95: 0 comments, control branch unchanged). #95 was NOT executed (it would edit Status Center). |
| Live write path | **BLOCKED (owner credential).** Smoke Issue #96 (`TASK: github-issues adapter smoke — read-only (safe)`) was created with the PAT, but the same PAT gets HTTP 403 on `POST /issues/96/labels` and `POST /issues/96/comments` → `cred_github_gateway` lacks `Issues: write` (creating the Issue only worked through permissionless public-repo access). #96 therefore has no `task` label and no adapter comment yet. |
| Owner steps | 1) fine-grained PAT `Issues: Read and write` on othoth77/mythos-prod → `/home/deploy/deployments/mythos-gateway/github-issues.env` (`MYTHOS_GITHUB_ISSUES_TOKEN=…`, 0600, `read -s`, `umask 077`), inventory entry `cred_github_issues`; 2) add label `task` to #96 (UI) and run as deploy from the worktree `node projects/mythos-ai-executor/bin/mythos-github-bridge issues-tick --only 96` twice (≈2 min apart, after the executor finishes) → created/claimed/report comments on #96; 3) merge the branch into main (`git merge --no-ff origin/mythos/github-issues-20260902` on the shared checkout, operator shell); 4) install the drop-in (`issues.conf.example` → `~deploy/.config/systemd/user/mythos-github-bridge.service.d/issues.conf`, `systemctl --user daemon-reload`) — ONLY after main carries the schema change; 5) decide #95: add `Action: implement` (or label `action:implement`) if the write run is intended, otherwise it runs read-only at the first enabled tick. |
| Not touched | AUTOS, SPY, n8n, MCP/ContextForge, OAuth, WhatsApp, production configuration, main, protected paths. |
| Housekeeping | #96 stays open as the smoke fixture until the write path is verified; the shared checkout `main` (d9479a1) is 2 behind `origin/main` (ccedcbc) — pre-existing, the relay reports it every tick. |

## 2026-09-02 — GitHub bridge WhatsApp notification layer (`gh-20260902-wa-bridge-notify-01`)

- Scope: added an optional, disabled-by-default WhatsApp notification layer to the existing MYTHOS GitHub bridge. Bridge notifications only — explicitly **not** MYTHOS AUTO WhatsApp / customer chat.
- Branch: `mythos/gh/gh-20260902-wa-bridge-notify-01`, based on `ccedcbc50850`. Dispatched from `control/tasks/gh-20260902-wa-bridge-notify-01.json`; OTHMODE record `OTH-2026-00024` (closed by the bridge, not by this session).
- Added: `projects/mythos-ai-executor/bridge/notify/whatsapp.js` (notification layer, idempotency ledger, retry), `bridge/notify/http-json.js` (Node-core JSON POST, no npm dependency), `bridge/notify/providers/evolution.js` (Evolution API adapter, `sendText` only), `tests/mythos-bridge-whatsapp-notify-test.js`, `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md`.
- Changed: `bridge/github-bridge.js` (one `require`, one enqueue call in `finishTask()`, `human_approval: true` on the blocked-for-a-human path, `flushNotifications()`, daemon flush), `bin/mythos-github-bridge` (`notify-config|notify-status|notify-flush|notify-test --confirm`), `docs/MYTHOS_GITHUB_BRIDGE.md` §12c, `projects/command-center/data/open-source-registry.json` (Search First verdict, append-only).
- Design: two-phase. `finishTask()` only writes a durable ledger entry (local, synchronous, **never network**); delivery happens in `flushNotifications()` **after** `tick()` has returned. A gateway outage therefore cannot slow, fail or alter a tick — verified by asserting the TASK and REPORT files are byte-identical and no control commit is produced while the gateway returns 500.
- Notifies on `COMPLETED`, `FAILED`, `BLOCKED` and `HUMAN_APPROVAL` only. `HUMAN_APPROVAL` is a notification kind for the bridge's existing blocked-for-a-human condition (claim exists, executor record gone); `mythos-control/1` and the control status set are **unchanged**.
- Idempotency: durable ledger keyed `<task_id>__<KIND>` under `$MYTHOS_BRIDGE_HOME/notify/ledger/` (existing executor store convention, no database added), with an `O_EXCL` lock per key. Duplicate polling, four parallel in-process flushes, four concurrent OS processes, and restart-after-crash all yield exactly one message per recipient.
- Tests run in this session: `mythos-bridge-whatsapp-notify-test.js` **116 passed / 0 failed** (new); `mythos-github-bridge-test.js` 97/0; `mythos-ai-executor-test.js` 265/0; `mythos-governance-invariant-test.js` 111/0; `othmode-3-tasks-test.js` 94/0; `othmode-2-platform-test.js` 141/0 (http section skipped: no `pg` on this host). `mcc-1-command-center-test.js` could not run — pre-existing `MODULE_NOT_FOUND: pg` in the untouched `projects/command-center/reference/db.js`, the same host limitation othmode-2 skips for.
- No real WhatsApp message is sent by any test: the far end is a local `http.createServer` on `127.0.0.1`, while the real adapter and real HTTP path are exercised.
- **Not done, deliberately:** the provider is **not deployed** and the one controlled real smoke test is **not performed**. No WhatsApp gateway exists on this host, the `deploy` user has no Docker access (`/var/run/docker.sock` permission denied), and swap is fully consumed (`free -m`: 4095/4095 MiB used, ~425 MiB RAM free) — provisioning a provider is a separate privileged deployment task and unsafe to attempt under that pressure.
- **Provider choice is PROVISIONAL.** This run had no outbound network access (`WebFetch`/`WebSearch` unavailable, direct HTTPS blocked), so upstream facts about Evolution API, WAHA, MultiWA and WaSphere could not be live-verified and are recorded `TO-VERIFY`. The mitigation is architectural: the provider sits behind an adapter, so switching provider — or migrating to the official WhatsApp Business Cloud API — is a new file plus one line in the `PROVIDERS` map, with `github-bridge.js` unchanged.
- Governance: no `git push` run (delivery is the `mythos-git-push` relay), nothing under `control/` edited, no governance-protected path touched, no unit file in Git carries a credential (configuration is a systemd drop-in outside the repository).
- Next step: the deployment task — address the swap pressure, live-verify the provider (licence, maintenance, real footprint) and re-open the choice if the footprint does not fit, deploy the gateway on a private network, configure the drop-in per `docs/MYTHOS_BRIDGE_WHATSAPP_NOTIFY.md` §4.1, then run `mythos-github-bridge notify-test --confirm` once.

**Last updated:** 2026-09-02 20:20 UTC
**From:** MYTHOS-GITHUB-ISSUES-0 — **GitHub Issues are now the official TASK intake.** `projects/mythos-ai-executor/bridge/github-issues.js` converts open Issues labelled `task` into `control/tasks/gh-issue-<n>.json` (PENDING, `source` block = the only schema addition), lets the UNCHANGED bridge/executor/OTHMODE run them, and posts created / claimed / report comments (status, summary, files, tests, commit SHAs, problems, next action) + status labels from the control files; HUMAN_APPROVAL derived from owner-decision BLOCKED reports; COMPLETED never closes an Issue unless `MYTHOS_ISSUES_CLOSE_ON_COMPLETED=1`; closing an Issue cancels its task; `rerun` label → new attempt. Idempotent (file-after-sync, shared lock, comment markers, hash-keyed rejections). Suites: issues 102/0 (fake GitHub API, real #95 payload parse-only), bridge 97/0, executor 265/0, governance-invariant 111/0. Live read path verified on #95 (dry-run: would create `gh-issue-95` as `investigate`; nothing written). **Live write path BLOCKED on a credential:** the only PAT (`cred_github_gateway`) gets 403 on comments/labels → owner issues a PAT with `Issues: Read and write` (see `docs/MYTHOS_GITHUB_ISSUES.md` §6) and installs the drop-in. Branch `mythos/github-issues-20260902` (`faf720c`, `4013781`, + this entry) on origin; NOT merged.**

## 2026-09-02 — Status Center discovery refresh

- Scope: refreshed `projects/status-center/data/repo-snapshot.json` from the current GitHub `othoth77` repository inventory.
- Verified new repository discovery: `othoth77/spy`, latest commit `41b3e04f53b401ca60380ba11a11a1c9fff1e871`, dated 2026-09-01T17:41:37Z, is now included in the snapshot and will surface as `NEW_DISCOVERY` until explicitly classified in the Status Center registry.
- Commit: `bceb38743ec442e7f28a46e5c7ceb997b9ba6185`.
- Remote `main` HEAD verified after write: `bceb38743ec442e7f28a46e5c7ceb997b9ba6185`.
- Verification: GitHub contents and commit metadata confirm the snapshot update is present on `main`.
- Limitation: the review engine could not be executed from this environment and the live VPS/status host could not be reached. Therefore no new immutable `REVIEW-*` snapshot, `data/current.json` publication, live `/health` verification, or production deployment is claimed by this handover entry.
- Next step: execute `node projects/status-center/bin/review.js`, verify the generated immutable review/current data, run `node tests/stc-1-status-center-test.js`, and publish the resulting Status Center snapshot through the sanctioned production deployment path.
