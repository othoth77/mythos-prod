# MYTHOS V3.2 — ecosystem intelligence, by connecting what V3.1 already runs

> **Status: IMPLEMENTED, MERGED AND LIVE-VERIFIED 2026-09-25** (#437–#444 + closeout; tracking #436).
> Evidence in §9; residuals with their exact reasons in §10.
> Architect/director for this version: Opus 5.5 (`claude-opus-5-5`) acting in the FABLE
> director role. Foundation: [V3_1.md](V3_1.md) (complete, running). A ticked box in §8 is
> true standalone or it is not ticked.

## 1. The finding that scopes V3.2

The working title was "AI system intelligence", with ten candidate capability areas. The audit
(§3) found that **every one of them already has a component in the repository** — a project
registry, an OTHKM graph, a reputation store, a roadmap selector, a dependency-waiting queue,
an event stream on the console. What is missing is not intelligence machinery. It is that
**four of those components are disconnected from the live path, stale, or unable to hold the
data they were built for**:

| Component | Built for | What the audit measured |
|---|---|---|
| `core/reputation.js` | learn which agent succeeds at which capability | the router **consults** it, but on the live Haddad path **nothing records an outcome** — only the core orchestrator (off) and the free-LLM pool write to it |
| `projects/oth-knowledge/lib/graph.js` | project/entity relationship graph | OTHKM has a `relationship` kind and `extract.addRelationship()`, but **the seed loader cannot load relationships** — V3.1 had to express "A uses B" as prose claims the graph cannot walk |
| `core/roadmap.js` | pick the next highest-value safe capability | its recorded state is **stale**: it recommends "Dependency DAG" first, and lists Agent Registry, Tool Registry, MCP Tool Layer, Validator, Worktrees, Reputation and Knowledge Graph as planned or conceptual — all exist in code with tests |
| `scripts/project-intelligence.js` | validate the project registries | **fails its own validation** (6 errors: six skills added 2026-08-26 were never registered), which is also the whole cause of the governance suite's long-standing "baseline" 33/3 |

So V3.2 is **ecosystem intelligence by connection**: make the existing components hold true
data, feed them from the live path, and give the director one read-only question it can ask
before building anything — *"do we already have this?"*

## 2. Roles (unchanged from V3.1)

FABLE director (the Claude session holding this document; decides, verifies, merges) · Qwen on
Haddad = default executor · Sonnet = standard-tier diagnosis · Opus = deep-tier diagnosis ·
OTHKM = the one memory · GitHub = record. **No Jev**: nothing in §4 needs a second decision
layer; model choice stays the executor's existing policy plus measured data.

## 3. Candidate areas — classified against the live system (2026-09-24)

| # | Candidate | Existing component(s) | Verdict |
|---|---|---|---|
| 1 | Project intelligence | `projects/meta/portfolio-registry.json` (36 tracks, capabilities, deps), OTHMODE `project_context`, `scripts/project-intelligence.js`, agent-skills registry | **IN — repair + extend** the existing tool (validation broken; no reuse query) |
| 2 | Cross-project knowledge | OTHKM namespaces (`projects/<slug>`), `othmode-memory.js`, seeds | exists; **fed by 3** |
| 3 | OTHKM project graph | `relationship` kind, `graph.js` (neighbors/walk), `context.js` | **IN — connect**: seed loader gains `relationships` |
| 4 | Reuse intelligence | `search-first` skill (prose), capability registry (skills/tools/providers), registry capabilities | **IN — one read-only query** over all of them (in the existing tool) |
| 5 | Research → execution continuity | GitHub Issues + bridge + handover; `role: researcher` tasks | EXISTS+WORKS — no change |
| 6 | Model intelligence | `config/agents.json`, `config/model-policy.json`, reputation | **IN — minimal**: measured limits on the agents that have them; observed performance comes from 8 |
| 7 | Execution intelligence | `core/reputation.js` + router `rankWithReputation`, per-task `report.json` evidence | **IN — wire** the executor's terminal seam to `recordOutcome`; summarise outcomes as data |
| 8 | Learning from results | same as 7 | IN, **as data only** — reputation counters and an outcome summary; no code or policy rewrites itself |
| 9 | Autonomous continuation | bridge queue with `depends_on` / `wait_dependencies`; `core/roadmap.js` selector (governance-caged) | queue EXISTS+WORKS; **selector state reconciled** so recommendations are true; creating new goals stays out |
| 10 | Live observability | Haddad console events (V3.1 #431), strict receiver allow-list on the VPS | EXISTS+WORKS; **one new event** (`outcome_recorded`) through the already-allowed events path |

Not in V3.2, by decision: an autonomous issue creator (it would be self-directed goal creation
— the continuation rule in §5 keeps the director as the one who files work); a second model; a
graph database; new console fields (the VPS receiver drops unknown fields until redeployed, and
events already carry what is needed).

## 4. Specification

### 4.1 OTHKM relationships in seeds (candidate 3)

- `projects/oth-knowledge/lib/seed.js` accepts `relationships: [{ from, to, rel_type, metadata? }]`
  where `from`/`to` are seed keys of already-loaded records; it calls the existing
  `extract.addRelationship()`. Unknown key → `OTHK_SEED_INPUT`; the secret gate already covers
  the whole seed. Idempotent like every seed record.
- `rel_type` vocabulary for the ecosystem: `uses`, `depends_on`, `reuses`, `can_execute`,
  `federates`, `serves`, `observes`. Free strings are accepted by the model (it is data, not a
  schema), the vocabulary is pinned in the test.
- The ecosystem seed gains typed relationships mirroring its relationship claims and the
  registry dependencies; `graph.neighbors()` then answers "what does X use / what uses X".

### 4.2 Project intelligence and the reuse query (candidates 1, 4)

`scripts/project-intelligence.js` (existing, dependency-free, read-only) is repaired and gains:

- `reuse "<need>"` — scores the need's words against: portfolio tracks (name, notes,
  `shared_platform_capabilities`), the agent-skills registry (`purpose`) and `.claude/skills`
  descriptions, the executor's agents/roles/skills/tools/MCP capability configs, and the
  committed OTHKM seeds (claims, entities, relationships). Prints JSON
  `{ kind: "data", query, matches: [{ source, id, file, score, text }] }`. **Output is labelled
  data**: it cites files and never contains an instruction the caller is meant to follow.
- `project <id>` — one project's identity, dependencies, dependents (reverse deps), reusable
  capabilities and its seed relationships (both directions).
- `outcomes [--store <dir>]` — read-only summary of an executor task store by provider × role:
  n, completed, failed, blocked, mean repair rounds, diagnosis rate, context-exhaustion count.
  This is how "learning" is represented: numbers the director reads, not behaviour that changes.
- the six unregistered skills are registered, so `validate` exits 0 and the governance suite's
  three "baseline" failures disappear.
- OTHMODE's `project_context` projection (`reference/othmode/registries.js`) additionally passes
  through `shared_platform_capabilities` and `notes` it currently drops (served on the VPS after
  the owner's pull).

### 4.3 Execution intelligence (candidates 6, 7, 8)

- `executor.js`: at the terminal seam, for a task whose provider supervises (reports a
  `validation` verdict), record `reputation.recordOutcome(<agent>, <capability>, success)` with
  the **same capability key the router ranks by** (`capabilities_required[0] || task_type`,
  from the resolved role), `success = validation.passed === true`. Tasks without a verdict
  record nothing (unknown is not failure). An `outcome_recorded` event carries
  `reason=<capability>:<pass|fail>` to the console.
- `config/agents.json`: measured limits on `haddad-qwen` (`context_window_tokens` 8192,
  `max_output_tokens_per_turn` 1536, `locality` local, `hardware`, `unit_of_work`) and
  `locality` on the others. Informational: nothing reads them for authority.
- **Authority does not move.** Reputation only reorders candidates the registry already
  selected; the bridge allow-list floor refuses anything outside it. A test drives a reputation
  store in which `claude-code` is perfect and `haddad-qwen` is failing, and asserts the Haddad
  decision is still `haddad-agent` or DEFER — never Claude.

### 4.4 Continuation (candidate 9)

- **Rule:** after a task, the next work is (1) the authorized queue — open Issues on an intake
  label, taken in order, dependencies waited on by the bridge; then (2) a *recommendation* from
  `core/roadmap.js selectNext()` over the committed Master Vision, which the **director** may
  turn into an Issue. Nothing files work by itself; governance capabilities are never
  selectable (`isGovernanceCapability`, unchanged).
- `config/roadmap-state.json` is reconciled with `recordProgress()` — which requires
  `{ commit, tests }` — for every capability the audit proved exists; PARTIAL where only part
  exists (e.g. Adversarial Reviewer: `review_fn` has no production implementation).

### 4.5 Security (applies to all of the above)

DATA vs KNOWLEDGE vs INSTRUCTIONS vs OWNER AUTHORITY:
- repository files, OTHKM records and reuse-query output are **data**; an OTHKM record is at
  most a **claim** unless an operator promoted it; neither ever carries authority;
- **instructions** reach the executor only as a labelled GitHub Issue on an intake label, parsed
  into a closed action set; the action decides the profile, never the text;
- **owner authority** is expressed in owner messages and the handover, never in OTHKM or project
  content (the permission layer refused recording it anywhere else during V3.1, correctly).
Tests in §7 pin: reputation cannot widen the allow-list; reuse output is data-labelled and the
tool performs no write and no network; seed relationships to unknown keys and secret-shaped
content are refused; governance roadmap capabilities stay unselectable.

## 5. Model policy for V3.2 work

Qwen executes what fits its measured unit of work (a new file under ~3 KB or a one-read one-write
edit with a quiet file-based acceptance — #428, #432-r3). The director edits the large files
(`executor.js` 1.8k lines, the 15 KB registry, `project-intelligence.js`). Sonnet/Opus are asked
only by the supervised loop's own escalation rule.

## 6. Plan

1. PR A — OTHKM relationships (seed loader + ecosystem relationships + tests).
2. PR B — project intelligence (skills registered, `reuse` / `project` / `outcomes`, OTHMODE
   projection, tests).
3. PR C — execution intelligence (executor seam, event, agents.json limits, authority test).
4. PR D — roadmap reconciliation, docs, handover.
5. Worker restart; live: outcome recorded by a real Qwen task; real cross-project E2E (§7).

## 7. Real cross-project E2E (required before any box in §8 is ticked for it)

A real Issue on the `oth-knowledge` intake (second Haddad project) whose task needs knowledge
owned by other projects (`mythos-haddad`, `mythos-gateway`, `mythos-ai-executor`): Qwen first
runs the reuse query in its sandbox, then writes a small OTHKM seed with typed relationships
using the loader capability added in PR A; acceptance = the seed loader in the sandbox. Director
reviews, merges, and the relationships become walkable by `graph.neighbors()`.

## 8. Completion checklist

- [x] OTHKM seeds carry typed relationships; `graph.neighbors()` answers uses/used-by for a project — #437, othk-22 54/0; the Qwen-authored graph answers the transitive question "what does mythos-gateway depend on"
- [x] `project-intelligence.js validate` exits 0; governance suite has no skills failure — #438, governance 33/3 → 36/0
- [x] `reuse` answers "do we already have X" across registry, skills, executor config and OTHKM seeds, output labelled data — #438/#440; asked live by Qwen inside its sandbox (#441-r3)
- [x] `project <id>` shows deps, dependents, capabilities and relationships — #438; `project mythos-gateway` now lists the Qwen-authored `depends_on` too
- [x] the executor records outcomes into the reputation store the router reads, live on Haddad — #439; live store `haddad-qwen/coding n=2 successes=1` from #441-r1 (fail) and #441-r3 (pass); the r2 runtime crashes recorded nothing, as designed
- [x] `outcome_recorded` visible on the public console — `status.mythosprod.xyz/data/haddad-node.json` shows `reason=coding:fail` (00:34Z) and `reason=coding:pass` (02:00Z)
- [x] outcome summary (`outcomes`) computed from the live store — coder n=7 (3 completed, 4 failed, 3 context-exhausted), researcher 3/3, tester 1/1
- [x] reputation cannot widen authority (test) — v32-execution-intelligence X6 (preference for Claude → DEFER) and X7 (no starvation); mutation-checked
- [x] roadmap recommendations are true (reconciled with commit + test evidence) — #442; the selector now recommends F Project Memory, not the built Dependency DAG
- [x] real cross-project E2E: Qwen used the reuse query and delivered a validated, merged seed — #441-r3, commit `b978ca2d` (author mythos-haddad-worker), mechanically verified, cherry-picked in the closeout PR
- [x] full sweep: no new failures — see §9
- [x] MERGED == RUNNING on Haddad — worker restarted on every code merge; identity checked each time (§9)
- [x] V3.2 decisions recorded in OTHKM seed form and in the handover — 5 decision claims in the ecosystem seed; handover entry 2026-09-25

## 9. Evidence (measured 2026-09-24/25)

| PR | What | Tests |
|---|---|---|
| #437 | spec + seed loader relationships, 19 typed relationships | othk-22 38 → 53 (mutation: loader skips relationships → 8 fail) |
| #438 | skills registered; `reuse` / `project` / `outcomes`; OTHMODE projection | v32-project-intelligence 8/0; governance 36/0 (was 33/3) |
| #439 | `recordAgentOutcome()` → reputation; `outcome_recorded`; agents.json limits | v32-execution-intelligence 7/0 (3 mutations caught) |
| #440 | `reuse --limit`; `outcomes` reads the real reputation path | v32-project-intelligence 9/0 |
| #442 | roadmap reconciled via `recordProgress` (8 implemented, 6 partial) | autonomous-campaign 365/0, governance-invariant 111/0 |
| #443 | **security:** project isolation (`write_scope`) | v32-project-isolation 8/0 (4 mutations caught) |
| #444 | **fix:** one validation baseline per attempt | v32-attempt-baseline 7/0 (reproduces the live failure) |

**Full regression sweep** (`tests/*-test.js` at the closeout commit, dedicated worktree, machine otherwise idle): **220 suites — 176 green, 38 NO RESULT (the harness-format count every baseline has had), 6 failing: hostops-daemon 5/9, hostops-executor 35/1, hostops 32/3, orchestration-core 255/2, v1-lane-routing 53/4, stage4w 42/2 — exactly the pre-V2 baseline set, minus mpi-0 governance, which V3.2 turned from 33/3 to 36/0.** Zero new failures. New V3.2 suites: v32-project-intelligence 9/0, v32-execution-intelligence 7/0, v32-project-isolation 8/0, v32-attempt-baseline 7/0, othk-22 54/0.

**Real cross-project E2E, three attempts, each one informative (Issue #441, `oth-knowledge` intake):**

| Attempt | Result | Cause | What it proved or changed |
|---|---|---|---|
| r1 | FAILED | execution 1 timed out while the director ran the full sweep (CPU contention); the retry re-snapshotted the worktree, so the file it had to create counted as MODIFIED (false integrity rejection), then context exhaustion | found the per-attempt baseline defect → #444; `outcome_recorded coding:fail` live; Sonnet diagnosis live |
| r2 | FAILED | three `vk::DeviceLostError` crashes of llama-server (nouveau/NVK) under 6–7k-token prompts; systemd restarted it each time | transient failures classified correctly and **recorded nothing** in reputation (unknown is not failure) |
| r3 | **COMPLETED** | smaller task body, same capability | Qwen ran `reuse` in its sandbox, wrote the seed, loader passed (6 entities, 7 relationships), **mechanically verified on attempt 1**; the file already existed from a pre-crash execution and was counted as **created** (the #444 fix, live); `project_scope` enforced, `out_of_project` empty (#443, live); constrained report turn fired (V3.1); `outcome_recorded coding:pass` (live store n=2, 1 success); delivered commit `b978ca2d` reviewed by the director (every relationship matches a registry dependency) and merged |

**Security audit (§4.5), what was checked and how:**

| Concern | Result |
|---|---|
| cross-project writes | **vulnerability found and fixed** (#443): a project never constrained its tasks' writes |
| validation baseline across retries | **defect found and fixed** (#444): an earlier execution's writes were invisible to a retry |
| learned data moving authority | not possible: X6 shows the router *does* prefer Claude with such data and the floor turns it into DEFER |
| OTHKM poisoning | AI writes only to staging (`propose.js`, capped at model-output); seeds enter only as reviewed commits; rel_type must be an identifier (prose refused); secret gate covers relationships |
| prompt injection via project files / OTHKM / reuse output | content reaches Qwen only as data inside its conversation; its tools are bounded by the action's profile and bwrap; the validator decides; reuse output is labelled data |
| continuation | recommend-only; governance capabilities unselectable (8); no code files work by itself |
| merge authority | executor delivery never pushes (push-guard 23/0); merges are the director's after the mechanical gate |
| sandbox / secrets | V3.1 adversarial probe stands; V3.2 added no new tool surface for the model beyond reading the reuse answer |

**Process errors of the director, recorded rather than hidden:** a first draft of item C was edited in the live checkout for a few minutes before being moved to a worktree (restored clean, no restart happened on it); a regression sweep was run during a live Qwen task and likely caused r1's timeout; several first-draft test assertions were decoration and were only caught by mutation (cached probes, equivalent roles, a test that called the helper itself).

## 10. Residuals, with the exact reason for each

1. **VPS ingest of the new seeds and `git pull` of the VPS checkout** — there is no Haddad → VPS path by design; the VPS bridge queue has been idle since 2026-09-17. Until the owner runs them, the canonical OTHKM store and OTHMODE's `project_context` do not show V3.2's relationships and capabilities (the repository and Haddad do).
2. **NVK `vk::DeviceLostError` under sustained 6–7k-token prompts** (5 runtime restarts on 2026-09-25) — a driver/hardware limit of the nouveau stack; recovered automatically every time. Mitigation that needs no install: keep Haddad task bodies small (r3 passed with half the bytes). Not a V3.2 defect.
3. **`review_fn`** — whether an LLM may review another LLM's standard-scope work is still an open owner decision; the review gate therefore stops completed work for a human (it did on #441-r3; the director reviewed and merged).
4. **Second bridge instance shares the `bridge/` cache** with the main instance — the one env line (`MYTHOS_BRIDGE_HOME=…/bridge-othk`) was refused to a session by the permission layer in V3.1; safe meanwhile because claims are keyed by globally unique issue ids.
5. **Qwen's measured unit of work** remains small (8,192-token window, whole-file writes): 3 of 7 coder tasks exhausted context. V3.2 makes this visible (`outcomes`) rather than pretending otherwise.
