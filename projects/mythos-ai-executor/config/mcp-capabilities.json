{
  "servers": {
    "github": {
      "enabled": true,
      "description": "GitHub pull-request review through github-mcp-rw, v1.10.1 tool names. Enabled by the owner on 2026-09-02; the credential is resolved by reference in lib/mcp-invoke.js and never reaches a task. create_branch was declared for the MCP-ECOSYSTEM-2b proof only and withdrawn on 2026-09-02 (least privilege: a review skill does not create branches). Flip enabled to disable the outbound half again.",
      "tools": {
        "list_pull_requests": {
          "description": "List pull requests for a repository (github.read)."
        },
        "pull_request_read": {
          "description": "Read one pull request: details, diff, files, comments, reviews, status (github.read; consolidated v1.10.1 name)."
        },
        "pull_request_review_write": {
          "description": "Create, add comments to, or submit a pull request review (github.pull_request, CONTROLLED: needs a GRANTED approval mcp:github.pull_request at invoke time)."
        }
      },
      "required_execution_profiles": [
        "repo-read",
        "repo-write"
      ]
    }
  }
}
