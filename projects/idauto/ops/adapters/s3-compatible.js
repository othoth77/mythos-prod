'use strict';
// S3-compatible HTTPS adapter (suitable for Cloudflare R2).
// User-local config (placeholders): ENDPOINT=https://example.invalid,
// REGION=auto, BUCKET=placeholder, ACCESS_KEY_ID=placeholder,
// SECRET_ACCESS_KEY=placeholder. Append-only or pull-based destinations are
// strongly preferred so a compromised source cannot delete both copies.
var fs=require('fs'),path=require('path'),os=require('os'),crypto=require('crypto'),https=require('https');
var DEFAULT_CONFIG=path.join(os.homedir(),'.config/mythos/idauto-offhost.env');
function sha(b){return crypto.createHash('sha256').update(b).digest('hex');}
function hmac(k,v,enc){return crypto.createHmac('sha256',k).update(v).digest(enc);}
function enc(s){return encodeURIComponent(s).replace(/[!'()*]/g,function(c){return '%'+c.charCodeAt(0).toString(16).toUpperCase();});}
function signingKey(secret,date,region,service){return hmac(hmac(hmac(hmac('AWS4'+secret,date),region),service),'aws4_request');}
function sign(p){var u=new URL(p.url);if(u.protocol!=='https:')throw new Error('non-HTTPS endpoint refused');var headers={};Object.keys(p.headers||{}).forEach(function(k){headers[k.toLowerCase()]=String(p.headers[k]).trim().replace(/\s+/g,' ');});headers.host=u.host;headers['x-amz-date']=p.amzDate;if((p.service||'s3')==='s3')headers['x-amz-content-sha256']=p.payloadHash;var names=Object.keys(headers).sort(),canonicalHeaders=names.map(function(n){return n+':'+headers[n]+'\n';}).join('');var qs=Array.from(u.searchParams.entries()).sort().map(function(x){return enc(x[0])+'='+enc(x[1]);}).join('&');var canonical=[p.method,u.pathname.split('/').map(enc).join('/'),qs,canonicalHeaders,names.join(';'),p.payloadHash].join('\n');var date=p.amzDate.slice(0,8),scope=[date,p.region,p.service||'s3','aws4_request'].join('/'),stringToSign=['AWS4-HMAC-SHA256',p.amzDate,scope,sha(canonical)].join('\n'),signature=hmac(signingKey(p.secret,date,p.region,p.service||'s3'),stringToSign,'hex');headers.authorization='AWS4-HMAC-SHA256 Credential='+p.accessKey+'/'+scope+', SignedHeaders='+names.join(';')+', Signature='+signature;return {headers:headers,canonicalRequest:canonical,stringToSign:stringToSign,signature:signature};}
function loadConfig(file){file=file||DEFAULT_CONFIG;var st;try{st=fs.statSync(file);}catch(e){throw new Error('configuration file missing or unreadable: '+file);}if((st.mode&0o777)!==0o600)throw new Error('configuration file must have mode 600: '+file);var out={};fs.readFileSync(file,'utf8').split('\n').forEach(function(l){var m=/^([A-Z_]+)=(.*)$/.exec(l);if(m)out[m[1]]=m[2];});['ENDPOINT','REGION','BUCKET','ACCESS_KEY_ID','SECRET_ACCESS_KEY'].forEach(function(k){if(!out[k])throw new Error('configuration file is missing '+k+': '+file);});if(new URL(out.ENDPOINT).protocol!=='https:')throw new Error('non-HTTPS endpoint refused');return out;}
function create(config,requestImpl){config=config||loadConfig();var base=new URL(config.ENDPOINT);if(base.protocol!=='https:')throw new Error('non-HTTPS endpoint refused');requestImpl=requestImpl||function(o,b){return new Promise(function(resolve,reject){var r=https.request(o,function(res){var a=[];res.on('data',function(x){a.push(x);});res.on('end',function(){resolve({status:res.statusCode,headers:res.headers,body:Buffer.concat(a)});});});r.on('error',function(){reject(new Error('provider unavailable'));});if(b&&b.length)r.write(b);r.end();});};async function req(method,key,body,extra){body=body||Buffer.alloc(0);var q='',raw=key||'',qi=raw.indexOf('?');if(qi>=0){q=raw.slice(qi);raw=raw.slice(0,qi);}var pathname='/'+enc(config.BUCKET)+(raw?'/'+raw.split('/').map(enc).join('/'):'')+q;var url=new URL(pathname,base).toString(),amz=new Date().toISOString().replace(/[:-]|\.\d{3}/g,''),s=sign({method:method,url:url,headers:extra||{},amzDate:amz,payloadHash:sha(body),region:config.REGION,accessKey:config.ACCESS_KEY_ID,secret:config.SECRET_ACCESS_KEY});var r=await requestImpl({method:method,url:url,headers:s.headers},body);if(r.status<200||r.status>=300)throw new Error(r.status===404?'missing object':'provider request failed');return r;}
return {put:async function(k,b,m){b=Buffer.from(b);var sum=m&&m.sha256||sha(b),r=await req('PUT',k,b,{'x-amz-meta-sha256':sum});return {size:b.length,sha256:sum,response:r.status};},get:async function(k){return (await req('GET',k)).body;},head:async function(k){var r=await req('HEAD',k),size=Number(r.headers['content-length']),sum=r.headers['x-amz-meta-sha256'];if(!Number.isFinite(size)||!sum)throw new Error('remote metadata missing');return {size:size,sha256:sum};},list:async function(prefix){var r=await req('GET','?list-type=2&prefix='+enc(prefix||''));return r.body;},capabilities:function(){return {put:true,get:true,head:true,list:true,delete:false,https_only:true};}};}
module.exports={create:create,loadConfig:loadConfig,sign:sign,signingKey:signingKey,DEFAULT_CONFIG:DEFAULT_CONFIG};
