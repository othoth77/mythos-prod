# `src/identity/` — NOT IMPLEMENTED

**Intended:** IVID issuance, DID resolution, and the issuer registry.

**Status:** empty placeholder. No code here.

**What exists today:** `../../reference/identity.js` — a minimal operator-provisioned map of admin bearer tokens to stable identity strings. Explicitly not an auth service.

See [`../README.md`](../README.md) for why this layout is reserved ahead of the code, and
[`../../docs/ROADMAP.md`](../../docs/ROADMAP.md) for when it is scheduled.
