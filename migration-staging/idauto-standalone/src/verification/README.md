# `src/verification/` — NOT IMPLEMENTED

**Intended:** The verification pipeline specified in `../../protocol/verification/README.md`.

**Status:** empty placeholder. No code here.

**What exists today:** Steps 1-4 of that pipeline are partly implemented across `../../reference/ingestion.js` and `../../reference/writes.js`.

See [`../README.md`](../README.md) for why this layout is reserved ahead of the code, and
[`../../docs/ROADMAP.md`](../../docs/ROADMAP.md) for when it is scheduled.
