# `src/api/` — NOT IMPLEMENTED

**Intended:** Protocol-shaped HTTP API — passport retrieval, claim submission, verification endpoints.

**Status:** empty placeholder. No code here.

**What exists today:** `../../reference/api.js` — the working HTTP API (read + write routes, admin-gated, not deployed publicly).

See [`../README.md`](../README.md) for why this layout is reserved ahead of the code, and
[`../../docs/ROADMAP.md`](../../docs/ROADMAP.md) for when it is scheduled.
